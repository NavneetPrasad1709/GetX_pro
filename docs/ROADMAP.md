# GETX — Build Roadmap (Next.js + Prisma stack)

We build **one step at a time**. Each step gets a ready-made prompt file in `docs/prompts/`.

How to use:
1. Open the current step's prompt file.
2. Copy its full content → paste to Claude Code.
3. Let Claude finish + run the QA checks at the bottom.
4. Tick the box here, then ask for the next step.

Legend: `[ ]` todo · `[x]` done · 🔑 = needs a token/credential (Claude will tell you exactly which)

---

## Phase 0 — Foundation
- [x] **Step 01** — Next.js setup (TS, Tailwind, shadcn, Prisma, folder structure, env) → `step-01-setup.md` ✅
- [x] **Step 02** 🔑 — Database schema + Neon connect + migrate + seed (users, sellers, games, listings, orders, escrow, chat, reviews, disputes, wallet) → `step-02-database.md` ✅
- [x] **Step 03** — Auth + roles (buyer/seller/admin), sessions, bot protection → `step-03-auth.md` ✅
- [x] **Step 04** — Design system + layout (dark gaming UI, mobile-first, GETX branding) → `step-04-design.md` ✅

## Phase 1 — MVP Marketplace (Months 1-3 · goal: first 100 sales · 5 games, Pokemon GO first)
- [x] **Step 05** — Game catalog (5 games) + categories (accounts/items/currency/boosting) → `step-05-catalog.md` ✅
- [x] **Step 06** — Seller onboarding (5-min) + create/manage listings → `step-06-seller.md` ✅
- [x] **Step 07** — Marketplace browse/search/filter + listing detail page → `step-07-browse.md` ✅
- [x] **Step 08** — Checkout + order creation flow → `step-08-checkout.md` ✅
- [x] **Step 09** 🔑 — Payments: CoinGate (crypto) + Razorpay (UPI) + webhooks + Sentry → `step-09-payments.md` ✅
- [x] **Step 10** — Escrow + delivery confirm + 3-day buyer protection + auto-release (Vercel Cron) → `step-10-escrow.md` ✅
- [x] **Step 11** 🔑 — Real-time chat (Socket.io server on Railway) → `step-11-chat.md` ✅
- [x] **Step 12** 🔑 — Image/file upload (Cloudflare R2) → `step-12-uploads.md` ✅
- [x] **Step 13** — Reviews & ratings → `step-13-reviews.md` ✅
- [x] **Step 14** — Wallet + seller payouts → `step-14-wallet.md` ✅
- [x] **Step 15** — Manual KYC + basic admin + manual dispute handling → `step-15-admin.md` ✅ — **🎉 MVP COMPLETE**

## Phase 2 — Growth (Months 4-12 · goal: 1000+ users · AI moats + retention)
> Note: the audit prompts (`docs/audit/prompts/` 11–25) already delivered several Phase-2 steps.
> Reconciled status below.
- [x] **Step 16** 🔑 — AI Support bot 24/7 (Claude API) → `step-16-ai-support.md` ✅ — floating streaming SSE widget (shop + dashboard, guests allowed) + server-side order/dispute context injection + SupportTicket escalation (user asks for human OR AI says "I don't know") + admin queue at `/admin/support`; consumes `src/lib/ai.ts`; degrades gracefully (503 + widget self-hides) until `ANTHROPIC_API_KEY` set; qa-step16-support.ts 47/47
- [x] **Step 17** — Live Trust Score (real-time via Socket.io + cron recompute) → ✅ built via **Audit Prompt-11** (trust-score.ts + 5 seller levels + cron + socket broadcast)
- [x] **Step 18** — AI Fraud Radar → ✅ fraud engine built via **Audit Prompt-16** (FraudFlag graph, signals, risk score, auto-actions); LLM scoring layer optional later
- [x] **Step 19** — Auto-delivery (instant codes/accounts) → `step-19-auto-delivery.md` ✅ — AES-256-GCM encrypted DeliveryItem pool, atomic assign at PAID (FOR UPDATE SKIP LOCKED), stockout→MANUAL fallback; qa 27/27
- [x] **Step 20** — Seller "CEO" dashboard → `step-20-ceo-dashboard.md` ✅ — `/seller/analytics` cockpit: Recharts revenue line + order-funnel bar + top-listings table, 4 stat cards (earned/available/escrow/orders), 7/30/90-day range, per-listing AI price suggestion (Haiku via ai.ts, graceful fallback). Reconciled to the wallet-based ledger (SALE credits, commission = sellerFeeMinor snapshot). qa-step20.ts 26/26. (Founder/admin analytics `/admin/analytics` was already built via Audit Prompt-19.)
- [x] **Step 21** — Loyalty points + referral → `step-21-loyalty-referral.md` ✅ — append-only `LoyaltyPoint` ledger (balance always derived ΣEARN−ΣREDEEM): 50-pt signup bonus, earn on every completed order (buyer 1pt/₹10, seller 1pt/₹20), redeem at checkout (server-clamped, discount comes out of the platform fee so escrow reconciliation + seller take are untouched), refund reversal (compensating EARN), /loyalty + /seller/loyalty dashboards + checkout redeem toggle. Referral half was already shipped (Audit Prompt-22, FEE_CREDIT) — kept as-is, no double-reward. qa-step21.ts 23/23 incl. the full buy→redeem→pay→release→refund money cycle.
- [x] **Step 22** 🔑 — Notifications (email via Resend + in-app bell + Socket.io push) → `step-22-notifications.md` ✅ — built via **Audit Prompt-18**
- [~] **Step 23** — i18n multi-language (next-intl) → `step-23-i18n.md` 🟡 **FOUNDATION built · URL-routing activation deferred to post-deploy** — shipped the durable, deploy-safe ~80%: `next-intl` installed + `src/i18n/{routing,request,navigation}.ts` (en+hi, `as-needed` prefix, missing-key→keypath fallback), **complete `messages/en.json`+`hi.json`** (12 namespaces, identical key sets, accurate Devanagari), `src/lib/format.ts` (locale-aware ₹/date), a ready `LanguageSwitcher` (unmounted). qa-step23.ts **13/13** (key parity, no-empty, Devanagari, ICU-placeholder parity, formatting); typecheck/lint/build green (foundation is inert → build unchanged). **DEFERRED** (3 reasons converged): a Windows file-lock blocked the `git mv` of the `(auth)` route group; the no-`[locale]`-move alternative (cookie locale) would globally deopt the Step 33 ISR/SEO right before deploy; and CLAUDE.md files i18n as post-MVP. Full activation checklist (route move → `[locale]/layout` → proxy chaining → string conversion → runtime QA) logged in `docs/DECISIONS.md`.
- [x] **Step 24** — Mobile-first PWA → `step-24-pwa.md` ✅ — installable PWA: `manifest.json` (standalone, #4d7cfe), hand-written Workbox `public/sw.js` (CacheFirst static / NetworkFirst API+nav→/offline / SWR images; skipWaiting+clients.claim), prod-only `SwRegister`, `/offline` fallback (zero DB/auth), iOS+Android `InstallBanner`, sharp-generated icons (96/192/512/180) + screenshots, layout PWA meta + `/sw.js` no-cache headers. qa-step24.ts 36/36. (Web Push deferred to Phase 3; next-pwa skipped — incompatible with Next 16/Turbopack.)

## Phase 3 — Market Leader (Year 2+ · dominate the niche)
- [x] **Step 25** — AI Dispute Judge (Claude + pgvector memory of past disputes) → `step-25-dispute-judge.md` ✅ — env-safe AI arbitration. `claude-opus-4-8` (via `ai.ts` `generateJSON` — **no `temperature`**, which 4.8 rejects) reads the full dispute context (order, listing, delivery proof, chat, reviews) → structured verdict (BUYER/SELLER) + confidence + reasoning + keyFacts; **confidence ≥ 70 auto-resolves** through the existing `escrow.resolveDispute` (single source of money truth — never a duplicate ledger write; AI actor = null, AuditLog.actorId is nullable), < 70 waits for an admin. **pgvector** case memory (`previewFeatures + extensions=[vector]`, `DisputeEmbedding` with `Unsupported("vector(1536)")` + ivfflat index) for few-shot retrieval; `embeddings.ts` uses **keyword vectors by default** (OpenAI via fetch when `OPENAI_API_KEY` set — dormant, no SDK dep added). Background enqueue (`setTimeout(0)`, no-op without a key), admin **AI Analysis** panel (verdict + confidence bar + accept/override → both move money via escrow). Reconciled the spec to the real schema (`resolveDispute(orderId)` not disputeId; `REFUND_BUYER/RELEASE_SELLER`; OrderDelivery.content; Review.rating). qa-step25.ts **29/29** (pgvector, auto-resolve ledger, idempotency, accept/override, no-key throw — via a test seam). 21-agent adversarial review → 8 fixes applied: a durable **`/api/cron/judge-disputes`** backstop (setTimeout(0) can be dropped by a frozen serverless lambda), atomic `judgeActorType` (folded into `resolveDispute`'s tx) + CAS-persist of AI fields, truthful `autoResolved` on re-judge, accept/override now revalidate the detail + order pages, cosine clamp to [0,1], OpenAI element validation, audit `actorType` meta, prod-guarded test seam.
- [x] **Step 26** — AI demand forecast + AI pricing → `step-26-ai-pricing.md` ✅ — `MarketSignal` daily aggregation (named distinct from the Prompt-12 `DemandSignal` email model) + `SearchLog`; `/api/cron/demand-signals` (fail-closed Bearer, idempotent upsert, `30 20 * * *`); `demand-forecast.ts` (Haiku demand forecast HIGH/MED/LOW + raise/lower/keep pricing, 1h cache, graceful null/low-data fallback); fire-and-forget search logging in `searchListings`; seller **Market Pulse** pills on /seller/listings; **Trending searches** added to existing `/admin/analytics` (GMV/top-games/categories already there from Prompt-19). qa-step26.ts 11/11.
- [x] **Step 27** — Community layer (guides, leaderboards, profiles, creator badges) → `step-27-community.md` ✅ — Guide/GuideView/GuideLike/Badge/UserBadge schema + 5 seeded badges; `badges.ts` (idempotent awards, milestone TRUSTED_VETERAN/EARLY_SELLER in releaseOrder, monthly TOP_SELLER cron, GUIDE_AUTHOR) + `guides.ts` (CRUD, view/like toggle, auto-publish for veterans); `/guides` + `/guides/[slug]` (XSS-safe react-markdown, no rehype-raw), `/leaderboards` + `/leaderboards/[gameSlug]` (30-day rankings + medals + badge strip), seller editor (textarea + live preview, NOT @uiw/react-md-editor), `/admin/guides` moderation (audit-logged), seller-profile badge row, `/api/cron/award-top-sellers` (fail-closed). qa-step27.ts 18/18.
- [x] **Step 28** 🔑 — Algolia search upgrade (when listings grow) → `step-28-search.md` ✅ — `algolia.ts` (admin client + isAlgoliaConfigured + record mapper, env-safe null) + `search-sync.ts` (syncListingToAlgolia fire-and-forget on create/update/setStatus/remove + bulkSyncAllListings 1000-batches) + setup-index script + `/api/cron/algolia-sync` (fail-closed, daily) + searchListings Algolia path (hydrate-from-Postgres, falls back to Postgres on any error/no-keys) + InstantSearchBar (graceful, null without keys). **Postgres stays the zero-config fallback** — Algolia activates when keys are set. qa-step28.ts 15/15.
- [x] **Step 29** 🔑 — Sumsub automated KYC + AML compliance → `step-29-sumsub.md` ✅ — env-safe automated KYC: `kyc-sumsub.ts` (HMAC-signed REST: createApplicant idempotent / generateSDKToken / getApplicantStatus + shared `applyKycReview` CAS), actions (getOrCreate + poll, SELLER-gated), webhook `/api/webhooks/sumsub` (fail-closed HMAC + Zod + state-guard idempotent GREEN→APPROVED/RED→REJECTED), embedded SDK widget on `/seller/verify` (falls back to manual upload when keys absent), admin Sumsub Cockpit deep-link. Manual admin override always available. qa-step29.ts 19/19. (Idempotency via KYC state-machine guard, not ProcessedWebhook — PaymentProvider enum stays payment-only.)
- [ ] **Step 30** — Add more games → `step-30-more-games.md`

## Phase 4 — Launch & Ops (cross-cutting, do before/at go-live)
- [x] **Step 31** 🔑 — Observability: Sentry + PostHog → `step-31-observability.md` ✅ — Sentry completed (configs already 0.1-prod from Step 09; added `setUser` after login, `SentryErrorBoundary` on dashboard + route `error.tsx`, admin-only `/api/debug/sentry-test`). PostHog wired (env-safe): server singleton + `captureServerEvent` (PII-stripped, `$ip:null`), provider + SPA pageview in root layout, and **6 events** (listing_viewed, checkout_started, payment_initiated, order_completed [server-truth], seller_onboarded, search_performed) at real call sites — IDs + amounts only, zero PII. All dormant without keys (Postgres/manual unaffected). qa-step31.ts 12/12; regressions 08/21/28 green.
- [x] **Step 32** 🔑 — Security hardening + rate limiting + Cloudflare WAF → `step-32-security.md` ✅ — env-safe hardening: `rateLimitDistributed` (Upstash sliding-window, falls back to the in-memory limiter when keys absent) wired on the brute-force surface (login/register/forgot/reset/authorize/pay-start); strong **static CSP** on every route via the Edge proxy (`csp.ts` builder; removed from next.config so one header per route; ISR-safe) with **nonce mode** built + dormant behind `CSP_NONCE_ENABLED`; **session revocation** — `User.sessionVersion` + jwt callback bounded re-check (≤60s, fail-open on DB error, returns null to sign out) bumped atomically on ban / role-change / password-reset (`sessions.ts` helper); webhook **IP allowlist** (`config/webhooks.ts`, open-by-default until provider IPs set) on Razorpay + CoinGate; HSTS/X-Frame/etc. kept in next.config; `npm audit` = 0 high/critical (4 moderate = postcss-inside-next, build-time only). 19-agent adversarial review: 5 false-positives rejected with library-source proof (incl. "proxy.ts not wired" — it IS, Next 16 compiles it to middleware; "null jwt-return crashes" — @auth/core guards it + clears the cookie = clean sign-out), 6 real fixes applied (Cloudflare CF-Connecting-IP, webhook call-time env read, test-DB safety guard, middleware skips static assets, limiter cap, CSP warn). qa-step32.ts 32/32.
- [x] **Step 33** — Performance (caching, bundle, CWV, query tuning) → `step-33-performance.md` ✅ — mostly a **reconciliation** (the codebase was already ~90% optimized: composite indexes, no N+1 in hot includes, ISR timings, fonts, bundle-analyzer, route-split recharts). New: `images.formats` AVIF/WebP; `/about` + `/how-it-works` marked `revalidate=false` (fully static); promoted **sharp** to a dependency (Vercel image optimization); removed unused **framer-motion**. Lighthouse/CWV is a CI/manual step (documented). qa-step33.ts 19/19.
- [x] **Step 34** — Testing (unit + integration + e2e) → `step-34-testing.md` ✅ — **Vitest** (node + jsdom toolchain) + **Playwright** configured; **39 unit tests** over the pure money/security math (fees, encryption, trust/risk score, order state machine, loyalty) with a **100% coverage gate** scoped to those modules (honest 80% threshold); **money-path integration suite** (createOrder→pay→deliver→confirm + refund) gated on `TEST_DATABASE_URL` (dormant until a Neon test branch at launch — runs in CI against an ephemeral Postgres); **E2E specs** (marketplace, auth/route-protection, mobile drawer) dormant until `playwright install` in CI; **`.github/workflows/ci.yml`** (quality → build+integration → e2e jobs, ephemeral Postgres service — no external accounts). `npm run test` = 39/39.
- [ ] **Step 35** 🔑 — CI/CD (GitHub Actions) + deploy (Vercel + Railway + Neon + Cloudflare) → `step-35-deploy.md`
- [ ] **Step 36** — Post-deploy checks + monitoring + seed 10 sellers (GO LIVE) → `step-36-golive.md`

---

> Prompt files are created **one step at a time** so they stay accurate to the latest code.
> Right now only `step-01-setup.md` exists. Finish it, then ask for Step 02.
