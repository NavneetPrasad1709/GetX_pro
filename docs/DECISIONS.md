# GETX — Decision Log

Har bada technical decision yahan ek line me (taaki future me "yeh kyun kiya" yaad rahe).

| Date | Decision | Why |
|---|---|---|
| 2026-06-05 | **Next.js 16 full-stack** (API routes, no separate backend) | Ek repo, best SEO+speed; create-next-app latest = 16 (App Router same as 15) |
| 2026-06-05 | **TypeScript strict + Tailwind + shadcn/ui** | Type-safe, mobile-first, fast UI |
| 2026-06-05 | DB: **Neon Postgres + Prisma** | Serverless + branch-per-feature (safe vibe coding); Prisma type-safe |
| 2026-06-05 | Realtime: **Socket.io on a separate Railway Node server** | Vercel serverless websockets hold nahi karta; Railway always-on |
| 2026-06-05 | Scheduled jobs: **Vercel Cron** (no Redis at MVP) | Escrow 3-day auto-release; Redis ki abhi zaroorat nahi |
| 2026-06-05 | Storage: **Cloudflare R2** | Sasta, S3-compatible, KYC docs + images |
| 2026-06-05 | Payments: **CoinGate (crypto) + Razorpay (UPI/INR)** | Stripe game-accounts ban karta — risk hata diya; crypto = global + never blocked |
| 2026-06-05 | AI: **Claude API** (`sonnet-4-6` default, `opus-4-8` for Dispute Judge) | AI moat = unfair advantage; build Phase 2-3 |
| 2026-06-05 | Money stored as **integer minor units** | Float me rounding bug |
| 2026-06-05 | Niche: **5 games, Pokemon GO first**; seed 10 sellers | Chicken-egg + scope-too-big death se bachna |
| 2026-06-05 | **Dropped Medusa/Mercur** in favour of Next.js+Prisma | Solo junior MVP ke liye Medusa over-complex; gaming logic custom anyway |
| 2026-06-05 | Auth: **Auth.js (NextAuth v5) + Prisma adapter** | Roles, ownership, session security for a money platform |
| 2026-06-05 | Money: **append-only LedgerEntry**, derive balances | Single balance field = money bugs; ledger = bank-grade truth |
| 2026-06-05 | Webhooks: **idempotent + signature-verified** | CoinGate/Razorpay resend events; dedupe stops double payouts |
| 2026-06-05 | **Sentry at payments step (09)**, not at launch | Payment bug without monitoring = blind |
| 2026-06-05 | Neon **pooled URL for app**, direct URL for migrations | Serverless connection exhaustion fix |
| 2026-06-05 | **Two-sided fees** (see `docs/FEES.md`): seller commission 6-8% + buyer platform 5%; ~13% take | Low seller commission = USP; total competitive w/ Eldorado |
| 2026-06-05 | Fees **configurable**, minor units, round-half-up; escrow holds subtotal+platform fee | Change rates without deploy; full buyer refund protection |
| 2026-06-05 | **Prisma pinned to v6** (not 7) | Prisma 7 forces driver-adapters + removes `url` from schema = over-complex for junior MVP |
| 2026-06-05 | Secrets in **`.env`** (not `.env.local`) | Prisma CLI only reads `.env`; Next reads it too — one file, less confusion |
| 2026-06-05 | Stack actuals: Tailwind **v4**, shadcn on **Base UI**, React **19** | What create-next-app + shadcn latest installed |
| 2026-06-06 | **Step 02 schema**: "seller" = `SellerProfile` (first-class), "buyer" = `User` | Listing/Order/Review/Conversation `sellerId` → SellerProfile; gives listing cards seller trust/rating in one join; wallet+payouts already hang off SellerProfile |
| 2026-06-06 | `ProcessedWebhook` unique = **composite `[provider, providerEventId]`** (not bare event id) | Two providers could reuse an event id; composite is the correct idempotency key |
| 2026-06-06 | `SellerProfile.ratingAvg` kept as **Float** (only money is Int-minor) | It's a 0-5 average, not money; Float is correct here |
| 2026-06-06 | `LedgerEntry.amountMinor` always **positive**; direction via `type` (CREDIT/DEBIT) + `balanceAfterMinor` snapshot | Bank-grade append-only ledger; signed amounts hide bugs |
| 2026-06-06 | `KycSubmission.reviewedBy` = soft String (admin userId), not FK | Avoids extra User relation for an admin audit field at MVP |
| 2026-06-06 | Auth.js tables (`Account`/`Session`/`VerificationToken`) added now, auth wired Step 03 | Adapter needs them in the DB before Step 03 |
| 2026-06-06 | **Step 03**: JWT sessions (not DB sessions) | Credentials provider cannot create DB sessions (Auth.js limitation); role lives in the token, refreshed via `unstable_update` on role change |
| 2026-06-06 | Password hashing: **bcryptjs cost 12** (pure JS) | Native bcrypt/argon2 cause build headaches on Windows + serverless; bcryptjs is zero-dep and Vercel-safe |
| 2026-06-06 | Login compares against a **dummy hash** when the email doesn't exist | Keeps response timing constant → blocks email enumeration via timing |
| 2026-06-06 | Verify/reset tokens stored **SHA-256 hashed**, single-use, TTL (24h/1h); reset tokens namespaced with `password-reset:` prefix | A DB leak never exposes usable links; a reset token can't be replayed as a verify token |
| 2026-06-06 | **Unverified users CAN log in, but cannot sell** | Spec says "verify before selling"; keeps buying friction low while gating the fraud-sensitive seller path |
| 2026-06-06 | Rate limit **inside authorize() too** (not only in the server action) | NextAuth's `/api/auth/callback/credentials` REST endpoint would bypass the action-level limiter + Turnstile — defense in depth |
| 2026-06-06 | Turnstile **fails closed**; `TURNSTILE_DEV_BYPASS=true` only works outside production | A money platform must never silently run without bot protection |
| 2026-06-06 | Route protection: **`src/proxy.ts`** (Next 16 middleware) = optimistic redirects only; real enforcement via `requireUser()/requireRole()` in layouts + actions | Middleware can be bypassed; the server-side check is the real gate |
| 2026-06-06 | Rate limiter is **in-memory fixed window** (per instance) at MVP | Approximate on serverless but stops naive brute force; swap for Upstash/Redis at Step 32 |
| 2026-06-06 | Social login: **Google + Discord OAuth**, each auto-disabled until its env keys exist | Discord = gamer audience natural fit; buttons render only for configured providers |
| 2026-06-06 | OAuth: `allowDangerousEmailAccountLinking: true` **but** signIn callback rejects provider-UNVERIFIED emails; successful link sets `emailVerified` | Same email = same account (good UX) stays safe — attackers can't squat an email they don't own; provider-verified email counts as verified for selling |
| 2026-06-07 | **Step 04 brand = neon lime `#aaff00`** primary (+ cyan `#3ee7ff`, violet `#9d7bff`) on dark `#07090f`/`#0e1422` | Documented brand (getx-strategy/guide) + existing code; the `docs/design-preview-v10.html` blue was an exploration only |
| 2026-06-07 | Fonts: **Sora** (display/headings) + **Inter** (body) + Geist Mono (mono, `preload:false`) via next/font | Sora = gaming/premium display; Inter = clean body; mono only below the fold |
| 2026-06-07 | Tailwind v4 theme in **`@theme` CSS tokens** (semantic shadcn + brand `--color-neon/cyan/violet/success`), dark-first in `:root`+`.dark`, `--radius 0.875rem` | One source of truth; brand utilities (`bg-neon`, `text-cyan`) auto-generated; app is always dark |
| 2026-06-07 | Marketplace/shared components are **presentational only** (`ListingCardData` prop type, no Prisma import) | SOLID — data mapped in later steps; cards reusable + testable without a DB |
| 2026-06-07 | Homepage hero is **static (server) + Inter heading**; Base UI Dialog drawer + sonner toaster **lazy via `next/dynamic`** | LCP element must not wait on framer-motion/webfont-swap; defer heavy client JS off the critical path |
| 2026-06-07 | **SUPERSEDED: brand is now v10** -- `docs/design-preview-v10.html` approved as FINAL; lime exploration dropped | Owner finalized the v10 direction after comparing previews |
| 2026-06-07 | **Brand v10: ONE blue accent `#4d7cfe`** (hover `#5e89ff`) + green `#45b483` (success) + star `#f0b429` on dark `#0a0b0d`/`#14161b`; white-alpha borders | Single accent = clean, premium, NOT cluttered (avoid G2G's mistake); green reserved for trust/success signals |
| 2026-06-07 | Fonts v10: **Poppins** (display + all UI chrome) + **Inter** (body copy) + JetBrains Mono (chips, `preload:false`) | Matches approved preview exactly; Poppins weights 400-800 self-hosted via next/font |
| 2026-06-07 | Logo = **real asset `public/getx-logo.webp`** via next/image (was text wordmark) | The approved preview uses the real logo; height-class sizing keeps aspect, zero CLS |
| 2026-06-07 | Homepage = v10 sections: hero (game-select search) -> browse-by-game (snap rail on mobile) -> protection steps -> why-GETX -> seller CTA -> reviews; **plain footer deleted, CinematicFooter (v10, with link columns) is THE site footer** | Approved design has one footer; curtain reveal degrades gracefully (<=900px + short viewports flow normally, reduced-motion skips GSAP) |
| 2026-06-07 | App chrome breakpoint = **900px** (`min-[901px]` arbitrary variants): trust ribbon + desktop nav above, bottom app nav + hamburger below | v10 treats phones AND tablets as 'app'; Tailwind md/lg (768/1024) straddle the design's single cutoff |
| 2026-06-07 | **Footer v10.1: dropped the 100vh fixed 'curtain reveal' + tilted marquee** -- footer now flows normally (strip -> CTA -> link columns -> bottom bar), marquee strip is straight, giant wordmark contained inside the CTA block | The fixed-height curtain squeezed all content into one viewport -> overlapping/confusing on laptop screens; normal flow can never overlap |
| 2026-06-07 | **Step 05: catalog 404s via segment layouts, NOT loading.tsx** — `games/[slug]/layout.tsx` + `[category]/layout.tsx` validate existence; segment-level `loading.tsx` removed at games/ + [slug]/ levels, slow listing queries stream via manual `<Suspense>` inside pages | Next 15.2+ streams metadata AND a parent loading.tsx flushes HTTP 200 before nested notFound() runs → bad URLs soft-404'd; layouts sit above every Suspense boundary so crawlers get a REAL 404 (SEO hygiene), while banner/categories render instantly (better UX than full-page skeleton) |
| 2026-06-07 | Step 05: out-of-range `?page=` = soft 404 (noindex), not real 404 | Layouts can't read searchParams; no internal link ever points past the last page, so crawl risk ≈ 0 |
| 2026-06-07 | Step 05: catalog reads via `src/server/services/catalog.ts`, all query fns React `cache()`-wrapped; only ACTIVE games/listings ever exposed | layout + generateMetadata + page share ONE Neon query per request; public surface can never leak DRAFT/PAUSED/REMOVED listings |
| 2026-06-07 | Step 05: per-game SEO/marketing copy in `src/config/games.ts` (not DB) | Hand-written copy for 5 launch games = code-reviewed, no migration, no unescaped user content in meta tags; `getGameCopy()` has a safe generated fallback for games added later (Step 30) |
| 2026-06-07 | Step 05: sitemap.ts revalidates hourly + falls back to static routes if Neon is down; robots.ts blocks /api,/admin,/dashboard,/auth-flows | A partial sitemap beats a 500; private surfaces stay uncrawled |
| 2026-06-07 | Step 05 a11y: footer column headings h5→h2; ListingCard aria-label removed (name = visible content) | Fixes Lighthouse heading-order + WCAG 2.5.3 label-in-name; **OPEN: v10 `--faint #6a6f7a` + white-on-blue CTA fail WCAG AA contrast (~3.7:1) — needs owner call since v10 palette is FINAL** |
| 2026-06-07 | Step 05 review fixes (multi-agent review, 12 confirmed findings): sitemap drops auth-gated `/become-seller`; Twitter cards added (`summary` until a real 1200×630 OG banner asset exists — **owner asset request**); OG images get width/height/alt; robots+noindex on /login,/register; `EmptyState` got `headingLevel` prop; pagination cells 44px on phones; v10 CTA extracted to shared `CtaLink`/`ctaVariants` (6 call sites); BrowseGames desktop grid reuses `GameCard`; `?page=` overflow clamps→soft-404 (was: silently page 1) | Sitemap must only list anonymous-200 URLs; one source of truth for the brand CTA (server-safe, no Base UI client island); `summary_large_image` would crop our square mark badly. Deferred to Step 12: `images.remotePatterns` for the R2 host (unknown until R2 bucket exists) |

| 2026-06-07 | **Contrast pass (owner-approved): two shades of the ONE v10 blue** — `--primary #4d7cfe` stays for accents/text/icons/rings/tints; new `--primary-strong #4169e8` (hover `#3a5fd9`, darkens) for ALL solid fills behind white text (CtaLink, Button default, Badge default, pagination active, header search, drawer sign-up, mobile-nav FAB); `--faint` → `#8a8f9a`; blue text on tinted/card surfaces uses the lighter `--primary-hover #5e89ff` (avatar initials, kicker chips on cards) | White-on-#4d7cfe = 3.73:1 (AA fail); as TEXT #4d7cfe passes on bg but not on lighter card tints. Global darkening would have broken ~40 small-text accents — shade-per-job fixes everything: Lighthouse a11y 100 + SEO 100 on home, /games, game page, category page |
| 2026-06-07 | **generateMetadata must not await heavy queries** — category page metadata no longer awaits the listings count (only the layout-cached game lookup) | Streamed metadata that resolves late lands after the crawler snapshot → Lighthouse saw "no meta description"; instant metadata lands in `<head>` |

| 2026-06-07 | **Step 06: listing `type` is DERIVED server-side from `Category.kind`** — the form's type field is UI-only; attributes are re-validated (and numerics coerced) against the derived kind inside the service | Client can lie; category is the source of truth. One Zod schema runs client AND server (price string → minor units via string math, never floats) |
| 2026-06-07 | Step 06: listing slug = slugified title + 4-hex-byte suffix, STABLE on edit; soft delete = status `REMOVED` (row kept); status machine: DRAFT→ACTIVE↔PAUSED→REMOVED, SOLD/REMOVED terminal | No uniqueness races/enumeration; listing URLs never break; order history (Step 08+) needs the rows |
| 2026-06-07 | Step 06: `becomeSeller` idempotent (race-safe via P2002 catch OUTSIDE the tx — Postgres aborts a tx after any error); + country + required terms checkbox | Double-submit/two-tabs returns the existing profile instead of a scary generic error |
| 2026-06-07 | Step 06: wallet balance ALWAYS from `getLedgerBalanceMinor` (credits − debits); dashboard's `cachedBalanceMinor` read + duplicate float formatter removed | Guardrails §1 — cache is never display truth; one shared service fn for both dashboards |
| 2026-06-07 | Step 06: authenticated write rate-limits key on **userId only** (never client IP) | X-Forwarded-For is attacker-controlled — mixing it into per-user keys lets an authenticated attacker mint fresh buckets and bypass the cap (multi-agent review finding) |
| 2026-06-07 | Step 06: listing images stay EMPTY until Step 12 (R2) — placeholder notice in the form, no URL input | Arbitrary remote URLs would crash next/image (no remotePatterns) and are an XSS/abuse surface; smart card fallback renders meanwhile |
| 2026-06-07 | Step 06: MVP listings are **INR-only**; native `<select>`/`<textarea>` primitives (no Base UI listbox); `/seller/**` gated by SellerProfile existence in the segment layout (role in JWT can be stale) | Multi-currency lands with payments (Step 09); native selects = zero JS + OS picker on mobile; profile row is the real permission |
| 2026-06-07 | Step 06 review fixes (21 agents, 12 confirmed / 4 refuted): empty stock now errors (was silent 0), aria-describedby everywhere, scroll-to-first-error + error summary, remove-confirm focus management, beforeunload guard, `(dashboard)/error.tsx`, `LISTING_TYPE_LABEL` centralized in config/games, `@@index([sellerId,status])` migration | Forms must be screen-reader + mobile honest; correctness/security/state-machine dimensions came back clean after the rate-limit + P2002 fixes |

| 2026-06-07 | **Step 07: search = Postgres `ILIKE`** (Prisma `contains`, insensitive) on title+description, debounced 350ms client-side | MVP catalog is tiny; full-text/trigram + Algolia is Step 28 ("Postgres search now, Algolia later"). Structured filters (game/type/price/delivery/trust/rating/currency) are the index-backed part |
| 2026-06-07 | Step 07: marketplace filter contract lives entirely in the **URL** (`q,game,type,min,max,delivery,trust,rating,currency,sort,page`); parsed/clamped by one tolerant `lib/validators/marketplace.ts` (never throws); prices travel as MAJOR ₹ in the URL, converted to minor units via string math | Shareable + SEO-ok results; one contract reused by page (parse), service (where/orderBy), client filter bar (control values), and chips/pagination (serialize) |
| 2026-06-07 | Step 07: `category/type` filter = **`Listing.type` (CategoryKind)**, game-independent (matches footer `?type=currency`); specific per-game category browsing stays on the existing `/games/[slug]/[category]` pages | Category slugs are unique only per game (`@@unique([gameId,slug])`) → a global `?category=` is ambiguous; kind is the clean cross-game facet |
| 2026-06-07 | Step 07: **currency filter = param + service supported, NO UI control yet** | MVP listings are INR-only (multi-currency lands with payments, Step 09); a single-option select is dead UI. Shareable `?currency=` still works server-side |
| 2026-06-07 | Step 07: faceted/searched/paginated marketplace variants are **`noindex,follow` + canonical `/marketplace`**; only the bare `/marketplace` is indexable | Avoids thin/duplicate faceted pages flooding the index; the dedicated `/games/[slug]/[category]` pages carry that SEO. Dynamic `<title>` per active filter still gives good tab/share previews |
| 2026-06-07 | Step 07: pagination uses an explicit **`{ id: "desc" }` tiebreaker** after every sort key; "newest" backed by new `@@index([status, createdAt])`; "top rated" backed by new `@@index([ratingAvg])` (trust sort already had `@@index([trustScore])`) | Offset pagination needs a TOTAL order or rows drop/duplicate across pages on a tied key; index every column we sort/filter by (guardrails) |
| 2026-06-07 | Step 07: listing detail uses ONE cache()-wrapped `getListingBySlug` shared by the **segment `layout.tsx` 404 gate** + `generateMetadata` + page body; no segment `loading.tsx` | Same streaming-404 rule as the catalog (metadata streams → layout is the only place a real HTTP 404 can be set); one Neon round trip; only ACTIVE listings are ever public (DRAFT/PAUSED/SOLD/REMOVED → 404) |
| 2026-06-07 | Step 07: buyer fee preview math lives in pure **`lib/fees.ts`** (`computeBuyerFee`, round-half-up via integer `floor((amount*pct+50)/100)`, minor units, reads `siteConfig.fees`) — client+server safe | One source of truth for the 5% buyer platform fee reused by the buy box (live preview) AND checkout/escrow (Step 08+); pure integer math, never floats |
| 2026-06-07 | Step 07: Product JSON-LD **omits `aggregateRating`** (offers/brand/sku/availability only); the seller's rating shows in the visible trust panel instead | Listing-level reviews arrive Step 13 — emitting the seller's rating as a *product* rating is misleading structured data (Google policy risk) |
| 2026-06-07 | Step 07: HeaderSearch wired as a **zero-JS GET `<form action="/marketplace">`** (game `<select>` from static `GAME_COPY` + `q` input); the interactive `/marketplace` filter bar is the ONLY client island (debounced search, render-phase setState resync, latest-`searchParams` ref to kill a debounce-vs-select race) | Keeps the LCP hero/drawer search server-rendered; "client JS minimal (only interactive filters)" per the step rules |
| 2026-06-07 | Step 07 review fixes (multi-dimension agents; 6 confirmed / 1 refuted): qty steppers 32→44px on mobile (WCAG 2.5.5), gallery `aria-current` boolean→`"step"`, `aria-busy` omitted when idle, out-of-stock `<span aria-disabled>`→`<button disabled>`, gallery key→index, `@@index([ratingAvg])`; debounce-vs-select stale-`searchParams` race fixed proactively. Refuted: gallery thumbnails already exceed 44px | a11y + perf hardening; correctness covered by `scripts/qa-step07.ts` (29/29: search/filter/sort/pagination/ACTIVE-only/detail/fee-math/parser) |

| 2026-06-07 | **Production-readiness audit** (acting as buyer/seller/CTO): 7 reports in `docs/audit/`. Verdict = NOT launch-ready (Overall 22/100, Launch 15/100) — the whole money path (Steps 08-15) is unbuilt; what exists is high quality | Honest, code-grounded gate before any real money. Drove the safety pass + Step 08 below |
| 2026-06-07 | **Audit safety pass**: (1) `git init` (repo was untracked — top ops risk; `.env` confirmed gitignored, `.gitattributes` LF-normalizes); (2) removed FABRICATED hero metrics ("4.9/5 from 12,400+", "₹2Cr+ escrow") + dropped the fake-testimonial reviews band (real reviews = Step 13); (3) baseline security headers + CSP in `next.config.ts` (X-Frame-Options/nosniff/Referrer/Permissions + frame-ancestors/base-uri/object-src/form-action; HSTS + upgrade-insecure-requests PROD-ONLY so localhost http dev isn't broken) | Fake social proof = trust/legal risk on a real launch; full nonce-based script/style CSP deferred to Step 32 (needs runtime verification vs Turnstile/OAuth) |
| 2026-06-07 | **UI evolution toward Eldorado-minimal + app-like** (within v10, blue+Poppins): added a "shop by category" mega-grid on home (scannable facet columns → `/marketplace?type=&game=`); added a **sticky mobile buy bar** on listing detail (persistent CTA, shares qty/total, hidden ≥901px); enabled cross-document **View Transitions** (progressive, reduced-motion safe). Evolves the "v10 FINAL" decision with owner approval | Owner liked Eldorado's minimal, category-first homepage + wanted an app-like feel; kept the v10 palette/fonts |
| 2026-06-07 | **Step 08 checkout + orders**: order **state machine** in `server/services/orders.ts` (`ORDER_TRANSITIONS`, `createOrder`/`transitionOrder`/`getOrder`); created in `AWAITING_PAYMENT`; **all money recomputed server-side from the DB** (client sends only slug+qty, never a price — guardrail §1/§5); `lib/fees.ts` `computeSellerCommissionMinor` snapshots the seller commission onto new `Order.sellerFeeMinor` (immune to later config drift); `totalMinor` = subtotal + buyer 5% fee (escrowed; gateway processing added at payment, Step 09) | Bank-grade: snapshot rates, in-transaction, no client trust |
| 2026-06-07 | Step 08: order creation **idempotent** = at most ONE open (`AWAITING_PAYMENT`) order per (buyer, listing); a re-checkout re-prices the SAME row (no duplicate). **Stock validated at creation but only DECREMENTED at payment** (Step 09) so an abandoned unpaid order never locks a one-of-a-kind listing | Double-click safety without permanent stock locks; payment-time decrement is the correct hold point |
| 2026-06-07 | Step 08: `/checkout` + `/orders` + `/orders/[id]` live under the `(dashboard)` auth layout; added to `proxy.ts` (optimistic redirect with `callbackUrl`); buyer-only fee breakdown per `docs/FEES.md` (seller commission NEVER shown to buyer); "Pay now" is a labelled placeholder until Step 09. `scripts/qa-step08.ts` 24/24 (money recompute, idempotency, edge cases, state machine, ownership) | Server-side `requireUser` is the real gate; payment provider wiring is the next step |

| 2026-06-07 | **Step 09 payments: plain REST (fetch) for BOTH gateways**, no provider SDKs | CoinGate's official Node SDK is unmaintained (last publish 2022); Razorpay's SDK only wraps 2 endpoints we need; fetch = full control over timeouts/errors, fewer deps on the money path |
| 2026-06-07 | Step 09: ONE shared `applyPaymentEvent` (services/payments/apply-event.ts) for both webhooks — dedupe via `ProcessedWebhook.createMany({skipDuplicates})` (`ON CONFLICT DO NOTHING`, no thrown-P2002 noise), order transition via **atomic CAS** (`updateMany WHERE status IN allowedFrom(to)`, derived from `ORDER_TRANSITIONS`), wallet row locked (`SELECT…FOR UPDATE`) before the ledger append | Exactly-once escrow under concurrent/duplicate/out-of-order webhooks — QA fires 3 parallel confirms, exactly 1 wins; the state machine is the single source of allowed moves |
| 2026-06-07 | Step 09: **CoinGate callbacks have NO HMAC** (verified in official docs) — authenticity = per-order random 32-byte `token` (stored on `Payment.webhookToken`, compared constant-time) AND the callback body is then DISCARDED: we re-fetch `GET /api/v2/orders/{id}` and only that authoritative response drives state | A leaked callback URL can't forge a payment; token + re-fetch is CoinGate's documented best practice. Razorpay = HMAC-SHA256(raw body, webhook secret) + `x-razorpay-event-id` dedupe |
| 2026-06-07 | Step 09: **CoinGate does NOT accept INR** (live 422, despite docs implying otherwise) → INR orders are invoiced in **USD at CoinGate's own merchant rate** (`/v2/rates/merchant/INR/USD`), rounded UP; `Payment` stores the USD charge + a `raw` snapshot `{forOrderTotalMinor, forOrderCurrency}` | Their rate = consistent with their books, no external FX dependency; round-up means the buyer can never underpay our INR total by conversion |
| 2026-06-07 | Step 09: CONFIRMED webhooks clear **two money checks** or are quarantined (`PAYMENT_AMOUNT_MISMATCH` audit + Sentry, order untouched): (a) event amount/currency == the CHARGE we created; (b) the Payment's order-economics snapshot == the order's CURRENT total (re-price drift guard) | A stale cheaper invoice paid after a re-checkout must never mark the order PAID; tampering/buggy events go to a human |
| 2026-06-07 | Step 09: **escrow hold = `order.totalMinor`** (subtotal + buyer fee, per FEES.md) as CREDIT `ESCROW_HOLD` on the seller wallet; `getWalletBalances` (services/wallet.ts) now splits **available vs held** (held = ESCROW_HOLD credits − ESCROW_RELEASE/REFUND debits); `getLedgerBalanceMinor` returns available only | Held money must never display as withdrawable; the formula already balances Step 10's planned release/refund entry pairs |
| 2026-06-07 | Step 09: stock decremented in the SAME transaction as PAID (conditional `updateMany stock >= qty`); stock→0 flips ACTIVE listing to SOLD; an oversold race still marks PAID but writes `LISTING_OVERSOLD` audit + Sentry | We HAVE the buyer's money — blocking PAID would strand it; admins get a loud flag instead |
| 2026-06-07 | Step 09: CoinGate has NO 'underpaid' status — partial payment surfaces as `underpaid_amount` on invalid/expired → mapped to our `UNDERPAID` (terminal-ish: support reconciles/refunds; buyer is NOT offered re-pay, avoiding accidental double payment). `payment.failed` (Razorpay) keeps the order payable (late capture can still arrive) | Matches real provider semantics instead of inventing states |
| 2026-06-07 | Step 09: Sentry v10 wired the CURRENT way — `src/instrumentation.ts` (`onRequestError`), `src/instrumentation-client.ts` (NOT deprecated sentry.client.config.ts), server/edge configs, `global-error.tsx`, `withSentryConfig`; DSN read explicitly from env (`NEXT_PUBLIC_SENTRY_DSN`/`SENTRY_DSN`), empty = fully silent; caught money-path errors call `captureException` + `flush(2000)` (serverless) | Auto-capture only sees UNCAUGHT errors; replay/feedback integrations skipped (bundle + PII caution) |
| 2026-06-07 | Step 09: webhook routes answer **401 forged / 400 garbage / 200 acknowledged-or-ignored / 500 ONLY transient** (CoinGate retries ~40×, Razorpay backs off 24h then disables the hook) | A retry can never fix a forged signature — only transient faults deserve the retry storm |
| 2026-06-07 | Step 09: payment-processing pass-through (FEES.md) **deferred** — gateways are charged exactly `totalMinor`; checkout's "added at payment" line now reads "included" | True pass-through needs per-method fee schedules (UPI vs card vs coin differ); an estimate would mis-charge. Revisit with real volume — OPEN item in FEES.md |
| 2026-06-07 | Step 09 review fixes (21-agent adversarial review: 4 confirmed / 10 minor / 3 refuted): (1) CoinGate `paid` WITH `underpaid_amount` ("Underpaid Cover" feature) → our UNDERPAID, never auto-escrow; (2) quarantine now DELETES its dedupe row (hold, not verdict — provider replay re-evaluates after a human fixes the cause); (3) `Payment.raw` updates MERGE under `lastEvent` so the creation-time drift snapshot is immutable; (4) CONFIRMED amount check fails CLOSED (null/unparseable gateway amount → quarantine, reason `unverified_amount`) | Each was a real money path: full escrow off a short payment, permanently unpayable orders, blinded re-price guard, unverified PAID. `scripts/qa-step09.ts` 72/72 incl. regression cases. Deferred-to-Step-32 minors: webhook IP rate-limit, redundant-invoice reuse lock |

| 2026-06-08 | **Step 10 escrow release = idempotent CAS** in `server/services/escrow.ts` (`releaseOrder`): atomic `updateMany WHERE status = DELIVERED → COMPLETED`; only the winner writes the ledger, so a double buyer-confirm OR a cron firing twice can NEVER double-pay (same pattern as Step 09's apply-event). Shared by `confirmReceipt` (buyer) + `runAutoRelease` (cron) | The single most expensive bug class here is paying a seller twice; the status CAS is the lock, proven by qa-step10 (double-confirm + double-sweep keep the ledger at 3 entries) |
| 2026-06-08 | Step 10 release ledger (docs/FEES.md): on COMPLETED, ONE tx writes **DEBIT ESCROW_RELEASE total + CREDIT SALE (subtotal − commission)** on the SELLER wallet, and **CREDIT FEE platformFee + CREDIT FEE commission** on a singleton **PLATFORM wallet**; defensive assert `sale + platformFee + commission === total` refuses to write money that doesn't reconcile | Seller's ledger stays clean (only their real money → available = sale, held = 0); GETX revenue is real, auditable double-entry rows; the assert is a tripwire against rounding/snapshot drift |
| 2026-06-08 | Platform wallet = **`Wallet.kind` enum (SELLER\|PLATFORM) + nullable `sellerProfileId`**, the GETX one identified by a FIXED id `"platform"`; get-or-create via `createMany({skipDuplicates})` (ON CONFLICT DO NOTHING) — NOT `upsert` | Schema needed a home for FEE revenue that isn't a fake seller; fixed id = guaranteed singleton; `createMany` is race-safe where two first-ever releases would make `upsert`'s find-then-create throw P2002 and poison the tx |
| 2026-06-08 | Delivery payload = new **`OrderDelivery`** model (1:1 with Order), content readable ONLY via `getOrder` (buyer/seller/admin gate); rendered as React-escaped `<pre>`, never `dangerouslySetInnerHTML` | Account creds are sensitive; one gated read path + no HTML injection. Plaintext at MVP per step scope — **encryption-at-rest + R2 attachments = documented follow-up** |
| 2026-06-08 | Step 10: `SellerProfile.totalSales` = **lifetime completed-sale COUNT** (+1 per release), matching the seed (120/60) + `seller-trust-panel` display | Resolves the schema's ambiguous field; it's a reputation count (shown as "N sales"), not a money sum |
| 2026-06-08 | Dispute (Step 10) = **buyer-opened** on a PAID\|DELIVERED order → DISPUTED (CAS), which freezes release because both auto-release + buyer-confirm require status DELIVERED; **Step 15 resolves** it (admin/AI) into COMPLETED or REFUNDED | Buyer-protection USP needs a freeze that's structurally impossible to bypass (the state machine itself blocks release), not a flag a race could miss |
| 2026-06-08 | `refund(orderId)` (used by dispute/admin — Step 15, no Step 10 UI) reverses the hold with **DEBIT REFUND total** (held → 0) from PAID\|DISPUTED only (never COMPLETED/ directly-DELIVERED), restocks +qty and reactivates an auto-SOLD listing; idempotent (2nd call = noop) | "All refunds reverse the hold in a transaction" (FEES.md); inventory returns honestly. CAVEAT logged: an oversold order (Step 09 `LISTING_OVERSOLD`, stock not decremented) would over-restock — admin reconciles that rare race |
| 2026-06-08 | Auto-release = **deadline timestamp (`autoReleaseAt` = deliveredAt + `siteConfig.escrow.autoReleaseDays` (3)) + periodic Vercel Cron sweep** (`/api/cron/auto-release`, `vercel.json` `*/15 * * * *`); each order released in its OWN tx (one failure can't roll back the rest), route auth = constant-time Bearer `CRON_SECRET`, **fail-closed** when unset | Guardrails §4 ("deadline + sweep, not a one-off job"); per-order tx + CAS = safe to re-run. NOTE: `*/15` needs Vercel **Pro** — Hobby allows daily crons only (coarsen the schedule or upgrade before launch) |
| 2026-06-08 | Step 10 reviewed by a **5-dimension adversarial agent workflow** (money/concurrency/auth-sec/state-machine/ui-a11y → refute-by-default verify): **0 confirmed findings** (1 raised, refuted). `scripts/qa-step10.ts` = 39/39 over the REAL paths (deliver→confirm→release reconciliation, double-confirm + double-sweep idempotency, ownership, dispute freeze, refund reversal, cron route 401/200) | Money path gets independent adversarial eyes before it ships; the platform-wallet P2002 race was caught + fixed in self-review pre-workflow |

| 2026-06-09 | **Step 11 chat = standalone Socket.io server in `socket-server/`** (deploys to Railway), separate from the Vercel app | Vercel is serverless and can't hold persistent websockets (CLAUDE.md architecture); the relay is a thin always-on Node process |
| 2026-06-09 | **Socket auth = short-lived (5-min) HS256 JWT** minted by the app (`src/lib/socket-token.ts`, pure Node `crypto`, shared `SOCKET_AUTH_SECRET`) + verified by the socket server (`socket-server/src/auth.ts`, an intentional COPY); client re-fetches a fresh token on every (re)connect via `auth:(cb)=>…` | Decouples the socket server from Auth.js internals/cookies; crypto JWT keeps the socket server dependency-light (no jose); re-fetch handles expiry across reconnects |
| 2026-06-09 | **Socket server holds NO business logic / NO DB** — it persists + authorizes by calling the app's SECURED internal API (`/api/internal/socket/{authorize,message,read}`, `Authorization: Bearer INTERNAL_API_SECRET`, constant-time + FAIL CLOSED in `src/lib/internal-auth.ts`) | "Keep the socket server thin; rules + persistence through the app" — one source of truth (Prisma + membership) stays in the Next app |
| 2026-06-09 | **Three-layer authorization:** handshake (WHO) → `conversation:join` calls `authorizeJoin` (membership) before joining the room → `message:send` requires being IN the room AND the app re-checks membership at persist (authoritative). Membership = `userId === buyerId \|\| userId === seller.user.id`; non-members get null/"not found" | Defense in depth — a forged join can't receive broadcasts, and even a spoofed in-room send is rejected at the authoritative persist check (no IDOR / cross-conversation leak) |
| 2026-06-09 | Conversations: **one general DM thread per (buyer, seller)** + **one per order** (tied by the unique `orderId`); `getOrCreateConversation` is idempotent (order race caught via P2002). "Chat with seller/buyer" lives on the listing page (logged-in non-owner) + order page | Reuses the Step 02 `Conversation`/`Message` models unchanged — no migration; order-tied chats keep delivery talk attached to the order |
| 2026-06-09 | **Optimistic send reconciled by `clientId`** (a pure per-instance counter ref — no `Date.now`/`Math.random` in render, React-Compiler-clean); `.timeout(8000)` ack marks a dropped send "failed" (retry); reconnect re-joins + backfills via `loadMessagesAction`; `key={conversationId}` remounts on thread switch | Snappy UX without losing the authoritative order; the server broadcast (real id + timestamp) replaces the temp; no forever-"sending" and no stale state across threads |
| 2026-06-09 | **Rate limit = 20 sends / 10s PER SOCKET** (`socket-server/src/rate-limit.ts`); message body coarse-capped 8000 on the socket, exact 2000 in `validators/chat.ts`; server actions rate-limited per userId. Bodies rendered as React-escaped text (never `dangerouslySetInnerHTML`) | Stops floods without hurting humans; the app is the precise validator; chat is a prime XSS surface so message text is always escaped |
| 2026-06-09 | Config: `NEXT_PUBLIC_SOCKET_URL` (public) + `SOCKET_AUTH_SECRET` + `INTERNAL_API_SECRET` shared across both `.env`s; socket server excluded from the app's tsconfig/eslint (own tooling). Local-run + Railway-deploy steps in `socket-server/README.md` | One source for the two shared secrets; the separate package can't pollute the Next build. NOTE: Step 32's nonce CSP must add the socket origin to `connect-src` |
| 2026-06-09 | Step 11 reviewed by a **5-dimension adversarial agent workflow** (socket-auth / authz-ownership / rate-limit / realtime-correctness / xss-exposure): **6 confirmed / 8 raised, 0 critical**. ALL FIXED: (1) `message:read` + `conversation:join` + `typing` now throttled by a SECOND per-socket limiter (60/10s — they each cost an internal HTTP+DB round-trip; only `message:send` was capped before); (2) reconnect-mid-send no longer duplicates — `backfill` de-ghosts optimistic "sending" rows by sender+body; (3) `backfill` now walks pages until overlap (no gap after a long disconnect, bounded 10 pages); (4) `/api/socket-token` rate-limited (30/min/user) so socket-churn can't bypass the send cap; (5) cross-process verifier agreement asserted in qa-step11 (catches token-verifier drift) | The two-process realtime layer turns every cheap socket event into a DB round-trip, so "throttle writes" (guardrail §7) must extend past `message:send`. `scripts/qa-step11.ts` 37/37 + `socket-server` relay 16/16. Self-review pre-workflow had already fixed thread-switch stale-state (`key`) + stuck-"sending" (ack `.timeout`) |

| 2026-06-09 | **Step 12 uploads = direct browser → R2 via presigned PUT** (`@aws-sdk/client-s3` + `s3-request-presigner`, `src/lib/r2.ts`); the `/api/uploads/presign` route validates type + size SERVER-SIDE (`checkUpload`) BEFORE signing, and signs `ContentType` + `ContentLength` so R2 rejects a body of any other type/size | Big files never proxy through Vercel (guardrail §6); signing the length/type binds the upload to what we validated, so a 100 MB or `.exe` body can't land on a small-image presign |
| 2026-06-09 | **TWO R2 buckets** — `R2_PUBLIC_BUCKET` (listing images, long-lived public URLs via `R2_PUBLIC_BASE_URL`) + `R2_PRIVATE_BUCKET` (KYC docs, NO public access). KYC read ONLY via a 120s presigned GET, **admin-role-checked first** + **audit-logged** (`KYC_DOC_VIEWED`); `KycSubmission.docUrl` stores the KEY, never a URL | Per-bucket access control is the clean way to keep listing images public but ID docs private (guardrail §6); admins-only + short TTL + access log makes PII reads traceable and un-leakable |
| 2026-06-09 | Object **keys are server-generated** from the caller's own `sellerId` (`listings/<seller>/<16-byte-hex>.<ext>`, `kyc/<seller>/…`) — the client never chooses the key/prefix; `submitKyc` re-checks the key sits under `kyc/<thisSeller>/` | A client-chosen key could target another seller's path or escalate into the private bucket; server-derived keys + prefix re-check close that |
| 2026-06-09 | Client-submitted listing image URLs are **re-validated server-side** (`isAllowedListingImageUrl`: must start with `${R2_PUBLIC_BASE_URL}/listings/`, no `..`, ≤2048) in `validateListingImages` (de-dupes, caps at 8); `next.config` `images.remotePatterns` scoped to the R2 host only; broken images degrade to the branded mono cover | Never trust a client URL on a record — blocks off-host/SSRF/content-injection via `next/image`; the listing carries URLs, KYC carries keys |
| 2026-06-09 | Uploads are **env-safe**: with R2 unset `isR2Configured()` is false and the presign route returns a clean **503** (mirrors Sentry/Turnstile empty-config). `scripts/qa-step12.ts` (presigning is local HMAC, so type/size/key/URL/KYC logic is fully testable without real R2; live browser→R2 PUT pending owner creds) | The build/dev never breaks waiting on credentials; the only untestable bit headlessly is the real bucket round-trip |
| 2026-06-09 | Step 12 reviewed by a **4-dimension adversarial security workflow** (presign-authz / KYC-privacy / injection-xss / size-type-bypass): **2 confirmed / 2 raised, 0 critical**, BOTH FIXED in `r2.ts` + locked by qa (43/43): (1) **HIGH** — SigV4 signs only `content-length;host`, so the code's documented "ContentType is bound" invariant was FALSE; a seller could presign `image/jpeg` then PUT `Content-Type: text/html` → R2 serves scriptable HTML at the `.jpg` public URL (content injection on our R2 domain). Fixed with `signableHeaders: {content-type,content-length,host}` in `getSignedUrl`. (2) **MEDIUM** — AWS SDK v3 default baked an EMPTY-body CRC32 (`x-amz-checksum-crc32=AAAAAA==`) into the presigned URL → R2 would reject every real PUT (BadDigest); fixed with `requestChecksumCalculation: "WHEN_REQUIRED"` on the S3Client | Both empirically reproduced against the installed SDK, not theory. The checksum landmine would have made uploads 100% broken in prod while QA (local HMAC) stayed green — the clearest reason the live R2 round-trip must still be run once creds land |

| 2026-06-09 | **Step 13 reviews are SELLER-level + verified-only**: only the BUYER of a COMPLETED order can review it, exactly once (`Review.orderId @unique`); eligibility + self-review block enforced in `reviews.ts createReview`. Reviews show on listing detail (latest 5 + link), the new public **`/sellers/[id]`** profile (paginated feed), and via the seller `ratingAvg`/`ratingCount` on the trust panel + ListingCard | Verified-purchase reviews are the trust USP; tying to a completed order makes fake reviews structurally impossible. No per-listing review column — a review is about the seller (matches the Step 07 "no product aggregateRating" call) |
| 2026-06-09 | Seller `ratingAvg`/`ratingCount` are **recomputed FROM the review rows in the same transaction** on every create/edit (`recomputeSellerRating`), guarded by `lockSellerForRating` (SELECT … FOR UPDATE on the seller row BEFORE the insert) | Cache can never drift from the rows; the FOR UPDATE serializes concurrent reviews for one seller so two simultaneous inserts can't each miss the other and leave the count short (self-spotted concurrency drift, fixed pre-review) |
| 2026-06-09 | Step 13 schema: `Review += sellerReply/sellerReplyAt` (one-time public seller reply, ownership = `review.seller.userId`), `updatedAt` (`@updatedAt`, drives the "edited" flag), `@@index([sellerId, createdAt])` for the feeds. Basic profanity guard (`lib/profanity.ts`, token-startsWith blocklist + leet) on comments + replies; rate-limited per userId | Seller reply = trust signal ("CEO" feel); migration `updatedAt` backfilled with `CURRENT_TIMESTAMP`. Profanity is intentionally basic (no Scunthorpe false-positive) — real moderation is admin/AI later |
| 2026-06-09 | `getSellerPublicProfile` returns `userId` but it's **SERVER-SIDE ONLY** (the seller-profile page uses it for the "is the viewer this seller?" → `canReply` check; never passed to a client component). `loadMoreReviewsAction` is public read-only (reviews hold no private data). `scripts/qa-step13.ts` 26/26 | The page passes only the boolean `canReply` + display fields to client islands; the userId never reaches the browser |
| 2026-06-09 | Step 13 reviewed by a **4-dimension adversarial workflow** (eligibility / aggregate / xss / ownership): **1 confirmed / 2 raised, 0 critical**, FIXED + locked by qa (27/27): MEDIUM — `replyToReview` enforced "one-time" only CLIENT-SIDE (form hidden when a reply exists), but the server action was directly callable → a seller could overwrite their public reply repeatedly (post polite, later swap hostile). Fixed with a server-side `if (review.sellerReply) throw` + a race-safe conditional `updateMany where sellerReply: null` (count 0 ⇒ already replied) | A "permanent on-record" reply must be enforced where it's written, not where it's rendered. The self-spotted aggregate FOR UPDATE + the eligibility/XSS/ownership dimensions came back clean |

| 2026-06-09 | **Step 14 payouts = admin-approved MVP** (no provider keys yet): seller requests → admin marks PAID/FAILED. Service (`payouts.ts`) is auth-agnostic so an automated **RazorpayX / CoinGate payout webhook is a drop-in later** (verify signature + dedupe → call the same idempotent `markPayoutPaid`/`markPayoutFailed`) | Manual flow is needed even WITH automation (admin oversight) and needs no token; building it first is never wrong. Automated payout is purely additive |
| 2026-06-09 | **Withdrawal RESERVES funds at request**: in a wallet-locked tx (`SELECT … FOR UPDATE` BEFORE reading available), write `DEBIT/PAYOUT` (available drops immediately) + create `Payout(REQUESTED)`. PAID = status only (DEBIT stands). FAILED = status CAS + `CREDIT/PAYOUT` reversal (restores available). Balance ALWAYS from the ledger (`getWalletBalances` now takes a tx client) | Reserving on request makes double-withdraw structurally impossible; the FOR UPDATE serializes concurrent requests so the 2nd sees the reduced balance (qa fires 2 parallel full-balance requests → exactly 1 wins). Escrow-`held` is excluded from `available`, so disputed/unreleased money is never withdrawable |
| 2026-06-09 | Payout processing is **idempotent via status CAS** (`updateMany WHERE status IN [REQUESTED,PROCESSING]`): only the winner acts, so a payout can never be both PAID and reversed, and a FAILED replay never double-credits. Min ₹500 / max ₹10,00,000 in `siteConfig.payouts`; admin actions role-gated (`session.user.role==='ADMIN'`), seller request rate-limited per userId | Same exactly-once pattern as the Step 09 escrow CAS; the reversal CREDIT is written only by the CAS winner. `scripts/qa-step14.ts` 23/23 (reserve, insufficient/min, concurrency, PAID no-op, FAILED reversal, double-reversal blocked, held-excluded, reconciliation) |
| 2026-06-09 | Seller `/seller/wallet` (Available / In-escrow / Withdrawing cards + filterable paginated ledger + withdraw form + payout history) + admin `/admin/payouts` (FIFO pending list → mark paid / fail+reverse). `LedgerEntry` carries no `payoutId` — the FAILED reversal is a compensating `CREDIT/PAYOUT` by amount (append-only; no row mutation), and the Payout table + PAYOUT ledger entries together tell the story | A `payoutId` FK would be cleaner but needs a migration; the compensating-entry model is correct double-entry and needs none. `availableMinor` is post-reserve, `pendingPayoutMinor` is the reserve shown separately (not double-counted) |
| 2026-06-09 | Step 14 reviewed by a **4-dimension adversarial money workflow** (double-withdraw / ledger-integrity / idempotency / authz): **0 findings — CLEAN**. The FOR-UPDATE-before-balance-read reserve, CAS-gated idempotent PAID/FAILED reversal, ledger-derived balance, and admin-gated processing all held up | The most money-critical surface yet came back with nothing to fix — the CAS + FOR UPDATE + append-only ledger patterns proven in Steps 09/10 transferred cleanly to payouts |

| 2026-06-09 | **Step 15 admin = control room** (ADMIN-only): `/admin` dashboard (stats + actionable queues) + `/admin/{disputes,kyc,payouts,orders,listings,users}`. Gated TWICE — `requireRole("ADMIN")` in the layout AND a server-side `requireAdmin` in every admin action (proxy is only an optimistic redirect). EVERY admin mutation writes an `AuditLog` with the actor | Defense-in-depth authz on the most powerful surface; the audit trail is the accountability backbone for a money platform |
| 2026-06-09 | **Dispute resolution is ATOMIC** (`escrow.ts resolveDispute`): ONE `db.$transaction` CAS-updates the Dispute (OPEN → RESOLVED_BUYER/SELLER) as the idempotency gate, then runs the money move in the SAME tx — `refundInTx` (DEBIT REFUND, reverse hold) for the buyer or `releaseOrder(tx, …, ["DISPUTED"])` (DEBIT ESCROW_RELEASE + CREDIT SALE + platform FEE) for the seller; a money "noop" rolls the whole thing back. `releaseOrder` was generalized to accept `allowedFrom` so the DELIVERED-release and DISPUTED-release share ONE code path | Either a dispute fully resolves (status + money together) or not at all — never a half-resolved dispute; re-resolving is rejected (qa proves it). Reuses the proven escrow ledger logic, so the dispute-release reconciles identically |
| 2026-06-09 | **Manual KYC review**: `getKycDocSignedUrl` checks ADMIN FIRST → 120s signed R2 GET → audit `KYC_DOC_VIEWED`; `reviewKyc` CAS a PENDING submission → APPROVED/REJECTED + sets `SellerProfile.kycStatus` + audit, in one tx. **User ban**: `User.bannedAt`; blocked at login in BOTH `authorize()` (credentials) + `signIn()` (OAuth, fail-closed); self-ban / ban-another-admin / self-role-change guarded. KNOWN MVP caveat: an existing JWT session persists until expiry (real-time revocation = Step 32) | KYC docs never leave the private bucket except via short-lived admin-only signed URLs; ban stops re-authentication immediately, the session-persistence window is the documented JWT trade-off |
| 2026-06-09 | **🎉 MVP COMPLETE.** `scripts/qa-step15.ts` 35/35 proves the FULL end-to-end loop: register → become seller → KYC approve → list → buyer pays (escrow hold) → deliver → confirm (release) → wallet → payout (admin paid) → review, PLUS dispute→REFUND-buyer and dispute→RELEASE-seller, PLUS admin moderation (ban/role/remove, all audited). Next: deploy + seed 10 Pokémon GO sellers (recommended) OR Phase 2 AI moats | Steps 01–15 done; the money path is bank-grade (append-only ledger + CAS + FOR UPDATE throughout), reviewed by adversarial agent workflows at every money/security step |
| 2026-06-09 | Step 15 reviewed by a **4-dimension adversarial workflow** (admin-authz / dispute-money / kyc-privacy / audit-ban): **1 confirmed / 0 critical**, FIXED + locked by qa (37/37): MEDIUM — a seller could hold MULTIPLE coexisting PENDING KYC submissions (submitKyc only blocked when already APPROVED), so an admin reviewing a STALE sibling could desync `SellerProfile.kycStatus` (e.g. flip an already-APPROVED seller to REJECTED). Fixed: `submitKyc` now locks the profile (`FOR UPDATE`) + refuses a new submission while one is PENDING → **at most one PENDING per seller**. admin-authz, dispute-money and audit/ban came back clean | Status-integrity bug, not a PII leak (the admin-only signed-URL gate + 120s TTL + audit were all intact). Same lock-then-recheck pattern as the wallet/rating concurrency fixes |
| 2026-06-09 | **Audit Prompt-01 — layout split into 5 shells** (Phase-2 restructure): root `layout.tsx` stripped to bare providers (html/fonts/DeferredToaster); chrome moved into per-group shells — `(marketing)` = SiteHeader + CinematicFooter + MobileNav (the ONLY group that renders the GSAP footer), new `(shop)/layout.tsx` = search-first `MarketplaceHeader` + `SlimFooter` (no GSAP), `(dashboard)` + `admin` = shared `AppShell` (AppTopbar + role-aware `AppSidebarNav` + app-variant MobileNav, no marketing footer), new `(checkout)` group = minimal `CheckoutHeader` (logo + green-lock) with its own `requireUser()`. Home moved root → `(marketing)/page.tsx`; checkout moved `(dashboard)/checkout` → `(checkout)/checkout`. `MobileNav` gained `variant` ("marketing"/"app") + `role` (app variant drops the Sell FAB); `appNav` added to `config/nav.ts` as the single source for sidebar + app bottom nav | The monolithic root layout (marketing footer/nav on checkout, dashboard, admin, login) was the UX audit's #1 defect (~20 downstream findings). Per-context shells match Eldorado/G2G/Fiverr; the GSAP CinematicFooter no longer ships in the app/checkout/admin route JS. All auth gates preserved (requireUser in dashboard + checkout layouts, requireRole("ADMIN") in admin, SellerProfile gate untouched); proxy matcher unchanged (route-group moves are URL-transparent). typecheck + lint (0 errors) + build all green |
| 2026-06-09 | **Audit Prompt-02 — marketplace nav + header search**: `mainNav` restructured to marketplace IA (Marketplace · Games · Sell · Help); new static `gamesNav` (built from `GAME_COPY`, category links use the live `/marketplace?game=&type=` contract — no DB at nav layer). New `GamesMegaNav` client island (hover/keyboard panel, Escape-to-close, 5 games × 4 category deep-links) rendered for the "Games" item inside marketing `NavLinks`. Existing zero-JS `HeaderSearch` promoted into the desktop **marketing** header (`hidden min-[901px]:flex`); mobile drawer gained a collapsible "Games" section (Games removed from its plain link list to avoid dup). `/sellers/[id]` breadcrumb fixed → Home › Marketplace › Seller | Reconciled with the Prompt-01 header split: mega-nav + search live on the marketing `SiteHeader`; shop pages already carry search via `MarketplaceHeader` (dashboard topbar search deferred to Prompt-06). Mega-nav is pure static data → header stays a zero-async server component, no client fetch, no new routes/params. typecheck + lint (0 errors) + build all green |
| 2026-06-09 | **Audit Prompt-03 — product (listing) page conversion fixes**: (1) **sticky desktop buy bar** via `min-[901px]:sticky min-[901px]:top-20` on the listing `<aside>` (no new DOM/JS — the whole right column pins; Buy CTA never scrolls away); (2) **stock urgency** in `BuyBox` — `LOW_STOCK_THRESHOLD=3`: `>3` → green "In stock" (CheckCircle2), `1–3` → amber "Only N left" (AlertTriangle), out-of-stock pill unchanged; (3) **review label** — subtitle clarifies seller-level (not listing-level) ratings + "Full seller profile" link, empty-state reworded ("…after your order is complete"); (4) **richer no-image cover** — `MonoCover` now blurs the game art (opacity-30 blur-sm, when `copy.image` exists) behind the monogram + "No photo yet", so it reads as branding not a broken image; (5) **removed the dead `/chat/new` link** from BuyBox (404 route) — the page-level `ChatWithSellerButton` (logged-in non-owners only, `openConversationAction`→`/messages/[id]`) is the canonical path | All UI/display only — zero money/escrow/order/auth changes. `BuyBox` props unchanged; `gameImage` threaded from static `config/games.ts` (safe `next/image` src, never user input); `canChat` gate untouched (server-side). typecheck + lint (0 errors) + build all green |
| 2026-06-09 | **Audit Prompt-04 — seller profiles as a conversion surface**: (1) `trustTone`/`trustLabel`/`formatReplyTime` consolidated into new pure `src/lib/trust.ts` — the 3 forked copies (listing-card, seller-trust-panel, sellers/[id]) removed (`grep "function trustTone"` → 1 hit); (2) new `<VerifiedBadge>` replaces the inline KYC chip duplicated in 2 places; (3) `getSellerResponseStats(sellerId)` added to `reviews.ts` — avg buyer→seller first-reply minutes from `Message.createdAt` over the last 20 conversations, `cache()`-wrapped, returns null below a 3-conversation sample (honest "New seller"); shown on the seller profile (3rd stat + header line) and the buy-box trust panel (passed as a parallel prop from the listing page, NOT baked into ListingDetail); (4) listing-card seller name is now a link to `/sellers/[id]` (id added to `ListingCardData.seller` + `listingCardInclude`/`toListingCardData`); (5) flat empty states → branded `<EmptyState>` (reviews + listings) | Used the **overlay-link pattern** (title `<Link after:inset-0>` + seller link `relative z-10`) instead of the prompt's nested-anchor + stopPropagation — nesting `<a>` in `<a>` is invalid HTML and breaks hydration; the overlay keeps the whole card clickable, the seller link independent, and the card a pure RSC (no client wrapper needed). Response stats are read-only aggregates keyed by sellerId only (no userId param, no message content) — no migration, no money/auth changes. typecheck + lint (0 errors) + build green |
| 2026-06-09 | **Audit Prompt-05 — checkout conversion** (the `(checkout)` minimal shell + CheckoutHeader were already built in Prompt-01): (1) **one-step pay** — new `CheckoutPayButton` client island calls `createOrderAction` then `startPaymentAction` in ONE transition (sequential, same press), copying `<PayNow>`'s exact Razorpay (`loadCheckoutJs`/`openRazorpay`, `theme #4d7cfe`, handler→`/orders/[id]?confirming=1`) + CoinGate (`window.location.assign`) launch logic — no intermediate `/orders/[id]` page load; (2) **sticky mobile pay bar** folded INTO the same component (shared `isPending`/`pay()` state — NOT a separate `CheckoutPayBar` that would need cross-component state sharing); (3) `CheckoutTrustBadges` (server) — escrow explainer (autoReleaseDays from siteConfig) + Escrow/Money-back/256-bit-SSL row; (4) consent links → `/terms` + `/refund-policy` (final paths, forward-ref Prompt-09); (5) **crypto poller timeout recovery** — `payment-status-poller.tsx` now flips to an amber "Still waiting" card with Check-again (resets ticks + refresh) + View-all-orders after ~2 min instead of a silently frozen spinner | **Golden rule held — zero payment/escrow/ledger/webhook/schema changes**: actions sent only `{listingSlug,qty,provider}` then `{orderId,provider}`, all money recomputed server-side, auth + per-user rate-limits intact. `createOrder`→`startPayment` are sequential in one `startTransition` (no race). `checkout-form.tsx` is now superseded by `checkout-pay-button.tsx` (orphaned, kept harmless). The old `(dashboard)/checkout` path was already removed in Prompt-01. typecheck + lint (0 errors) + build green |
| 2026-06-09 | **Audit Prompt-06 — dashboard app shell** (reconciled with Prompt-01: the footer/chrome suppression the prompt's data-shell CSS aimed for is ALREADY solved by the layout split, so I enhanced the existing `app-shell/app-topbar/app-sidebar` instead of creating duplicate `dashboard-*` files): (1) `AppSidebarNav` fleshed out into a **role-aware** sidebar — buyer base (Dashboard/Orders/Messages/Settings-soon) + Seller Hub group (Overview/Orders/Listings/New-listing/Wallet/Verify/Analytics-soon) + Admin group (Control room/Disputes/KYC/Payouts/Orders/Listings/Users), active-state + icons; (2) `AppTopbar` gained a disabled "Search GETX" placeholder (desktop, wired later); (3) **KYC banner** — new `KycStatusBanner` (NONE/PENDING/REJECTED → amber/blue/red, APPROVED→null) rendered hub-wide for sellers via AppShell, fed by a single `kycStatus` read in the dashboard layout; (4) **order status tabs** — new `OrderStatusTabs` (Link-based **server** component, zero JS) + `parseOrderFilter`; buyer & seller order pages now compute 5 counts in JS and filter via `?filter=`, Needs-action banner driven by the count; (5) **mobile Orders badge** — `MobileNav` gained `ordersBadge` (from new `getActionRequiredCount` = DELIVERED-as-buyer + PAID-as-seller), shown on the app-variant Orders tab; (6) seller layout slimmed to a pure gate (nav moved to sidebar), admin layout drops the horizontal AdminNav (sidebar covers it), motivating empty-state copy on buyer-orders + seller-listings | No migration, no money/escrow/auth changes — `requireUser`/`requireRole("ADMIN")`/SellerProfile-gate all preserved; new reads are ownership-scoped indexed counts. Tabs are SSR `<Link>`s (no client island, no history spam). `admin-nav.tsx` + `seller-nav-links.tsx` now orphaned (kept, harmless). Deferred: seller-wallet empty-state copy (minor, low-traffic). typecheck + lint (0 errors) + build green |
| 2026-06-09 | **Audit Prompt-07 — mobile experience** (reconciled with 01/06): (1) **bottom nav redesign** — `mobileNav` is now Home/Browse/**Messages**/Orders/Account; the one-time Sell FAB + its special-case branch deleted; `MobileNav` gained `unreadMessages` (badge on Messages, wired from the marketing layout via `auth()`+`countUnread`) alongside the Prompt-06 `ordersBadge`; (2) **buy-box sticky bar** got `paddingBottom: env(safe-area-inset-bottom)` (iPhone home-indicator safe); (3) **role-aware drawer** — `MobileDrawer`+`DrawerDialog` take a `role` prop (passed from SiteHeader's server `auth()`); seller/admin see "Seller hub" (+ admin sees "Admin panel"), buyers see "Start selling"; logged-out unchanged. **Deferred:** Task 2 marketplace filter **bottom-sheet** (large, isolated — focused follow-up). **Moot after 01/06:** Task 3 chat-height (chat-room has no `100dvh-9rem` calc; it scrolls inside the app-shell main which already clears the nav via pb-[74px]) and Task 5 sub-nav scroll (`seller-nav-links`/`admin-nav` were replaced by the sidebar in Prompt 06 — orphaned, not rendered) | No money/escrow/auth/chat-logic touched; `role` read server-side only (never client/URL). Also shipped **`docs/ui-preview.html`** — a self-contained v10-styled visual of every 01–07 UI change so the owner can SEE the work in a browser without running the app. typecheck + lint (0 errors) + build green |
| 2026-06-09 | **Audit Prompt-09 — trust signals + legal pages**: (1) Removed fabricated "12,400+ gamers" metric from `CinematicFooter` (false-advertising risk; contradicts project's anti-fake-metrics policy); stripped fake aggregate ("4.9★ / 2,300+") from dormant `HomeReviews` component; (2) Built **13 marketing/legal pages** under `(marketing)` route group — priority legal blockers: `/terms`, `/privacy`, `/fees`, `/refund-policy` (required for Razorpay + CoinGate merchant activation); content pages: `/how-it-works`, `/help`, `/contact`, `/about`, `/seller-guide`, `/payouts`, `/trust-safety`; stubs: `/blog`, `/careers`; all static RSC with `generateMetadata`, `PageContainer`, `Breadcrumbs`, real honest content pulling from `siteConfig`; (3) Added `kycVerified: boolean` to `ListingCardData` (derived server-side from `kycStatus === "APPROVED"` in `toListingCardData`); green `Verified` badge via `BadgeCheckIcon` in the listing card seller row; (4) `SellerTrustPanel` new-seller trust state — shown when `totalSales === 0 && ratingCount === 0`; `avgFirstReplyMinutes` "New seller" fallback removed in favour of the explicit trust-positive paragraph; (5) Homepage hero: `Browse listings` `CtaLink` (full-width mobile / auto desktop) between search bar and trust badges; (6) Trust-positive empty review state on listing-detail page (two-line structure vs single grey paragraph); (7) `sitemap.ts` expanded with all 13 new static pages at correct priorities (legal 0.5, content 0.6, seller 0.7, stubs 0.4) | Payment-processor activation (Razorpay/CoinGate require live T&C + Privacy + Refund Policy URLs); verified badge on cards matches Eldorado/G2G conversion benchmarks; fake metrics = legal + trust risk. typecheck (0 errors) + build green |
| 2026-06-09 | **Audit Prompt-08 — search & filters** (Postgres-only; Algolia is Step 28): added a **"Most popular"** sort (`SellerProfile.totalSales DESC, ratingAvg DESC, id`), a one-tap **"⚡ Instant only"** quick-toggle above the panel, **"Seller experience"** (`minSales` 10/50/100+) + **"Verified only"** (`kycStatus=APPROVED`) filters, and **game + type facet counts** (e.g. "Pokémon GO (12)") via a new `getFacetCounts` (two `groupBy` + one game-name lookup in one tx; game counts strip the game dim, type counts keep game scope). All URL-first + backward-compatible (new params parse to `undefined` when absent; faceted variants stay noindex). Schema: `@@index([totalSales])` on SellerProfile + migration `20260609140000_prompt08_popular_sort_index` (CREATE INDEX IF NOT EXISTS — **apply at deploy via `prisma migrate deploy`**; code works without it, just seq-scans at current volume) | All new filters parsed server-side with range clamps (`parseIntInRange`/`"1"`-only verified) — no raw input to Prisma; `getFacetCounts` reuses `buildWhere` so the ACTIVE guard applies to counts; no money/auth/schema-data change. Prisma `groupBy._count` is loosely typed → coerced with a `countOf` number guard. typecheck + lint (0 errors) + build green |

| 2026-06-10 | **Audit Prompt-17 — marketplace SEO (Eldorado-grade organic engine)**: all 9 tasks — (1) **Sitemap** now includes every ACTIVE listing + every non-banned seller profile (`lastmod`=updatedAt, capped at 45k with a split-to-index TODO; fail-closed preserved); (2) **AggregateRating** wired into listing Product JSON-LD (seller's aggregate, only at ≥3 reviews per Google's minimum); (3) **Organization JSON-LD** in root layout (`OrganizationJsonLd`, static + escaped, social `sameAs` from nav.ts) for entity authority; (4) **`GameCategoryCopy`** + `getGameCategoryCopy()` in games.ts — hand-written copy for pokemon-go/accounts + /pokecoins, genuine parameterized fallback for the rest (intro + body paragraphs + FAQs); (5) **category pages** render the SEO `<article>` + zero-JS `FaqAccordion` (`<details>`) + **FAQPage JSON-LD** (only when items>0); (6) **internal linking** — `getMoreFromSeller` + `getMoreInCategory` (cache(), ACTIVE-only, parallel with reviews — no waterfall) render "More from {seller}" / "More in {category}" rails (≥2-result guard); (7) **footer** gained "Games" (all 5 + index) + "Top categories" columns (site-wide crawl equity; 6-col grid); (8a) `/games` got a Breadcrumbs + JSON-LD trail; (8b) **seller profiles** got keywords (from active-listing games), OG-route image + `summary_large_image`, Games breadcrumb crumb; (9) **dynamic OG images** — `/og/listing/[slug]` + `/og/seller/[id]` via `next/og ImageResponse` (1200×630, branded, DB-validated with generic fallback for unknown ids, `revalidate=86400` + Cache-Control, NO remote-image fetch = crash-proof); listing + seller metadata now use these + `summary_large_image`. **Reconciliation:** skipped the spec's 100ms Promise.race on related queries (they already run in the page's parallel Promise.all — no added waterfall; an aggressive race would just blank the rails); OG cards use a branded layout instead of embedding the remote listing image (robustness > fidelity — a broken R2 URL can't crash the route). robots.ts leaves `/og` crawlable; OG routes not in sitemap. typecheck + lint (0 errors) + build green | Organic is Eldorado's deepest moat; full listing/seller sitemap + rich results + category copy + session-depth rails + social OG cards close the gap in a focused 5-game niche |
| 2026-06-10 | **Audit Prompt-07b — marketplace filter bottom sheet** (deferred Task 2 from Prompt 07): replaced the inline filter accordion (which pushed listing cards off-screen at 390px) with a Base UI `Dialog` bottom sheet on mobile. Extracted the control groups into a shared **`filter-controls.tsx`** (reused by BOTH the desktop inline grid and the mobile sheet — `idPrefix` keeps DOM ids unique, no duplication); new **`filter-sheet.tsx`** (slides up from bottom, `max-h-[80dvh]` so listings stay visible behind it, drag-handle, `env(safe-area-inset-bottom)` padding, sticky footer with Reset → `/marketplace` + `Apply (N)` Dialog.Close). `MarketplaceFilters`: `panelOpen`→`sheetOpen`, Filters button opens the sheet, desktop panel kept as `hidden min-[761px]:grid`, `FilterSheet` **dynamically imported** (`ssr:false`) so it stays out of the marketplace LCP path; `pushParams` wrapped in `useCallback`. Filter changes still write to the URL immediately (Apply = just close). typecheck + lint (0 errors) + build green | Mobile filtering matched Airbnb/G2G standard (listings visible while filtering); the deferred follow-up from Prompt 07 is now closed |
| 2026-06-10 | **Audit Prompt-15b — revenue Streams 3/5/6/7** (completes Prompt 15): (1) **Schema** `20260610400000_step15b_revenue_streams` — `SellerProfile.isSponsored/sponsorshipExpiresAt` (+index), `Order.hasShield/shieldFeeMinor`, `Payout.isInstant/instantFeeMinor`, `Listing.bumpedAt/bumpCount/lastBumpAt`, `LedgerReason += INSTANT_PAYOUT_FEE`; (2) **Stream 3 Spotlight** — `sponsorSeller` (reuses `chargeSellerToPlatform`; gated: KYC-approved, ≥5 sales, 4.0★+, no open disputes, ≤3 active slots; re-buy extends a week) + `getSponsoredSellers` + homepage `SellerSpotlight` "Sponsored" rail + seller-hub card + `SponsorButton`; expiry handled in the subscription-renewal cron; (3) **Stream 5 Shield** — `computeShieldFeeMinor` + `computeBuyerFee(…, {shield})` returns shieldFeeMinor/hasShield; `createOrder` snapshots `hasShield`+`shieldFeeMinor` into totalMinor; `markDelivered` uses `shield.extendedEscrowDays` (7) vs 3 for the auto-release window; BuyBox toggle (live total) + checkout shield line + `?shield=1` wiring through CheckoutPayButton→createOrderAction; buyer-favored disputes already refund full totalMinor (shield fee included); (4) **Stream 6 Instant payout** — `computeInstantPayoutFeeMinor`; `requestPayout(…, isInstant)` checks `amount+fee ≤ available`, DEBITs PAYOUT + INSTANT_PAYOUT_FEE on seller + CREDIT FEE platform in ONE tx (wallet already locked), stores `Payout.isInstant/instantFeeMinor`; form toggle (live fee) + admin INSTANT badge; fee earned at request (not reversed on failed payout — documented); (5) **Stream 7 Bumps** — `computeBumpFeeMinor`; `bumpListing` (BOOST_FEE charge; max 3/listing/24h via AuditLog count; sets `bumpedAt`/`bumpCount`/`lastBumpAt`); marketplace `newest` raw SQL now `ORDER BY <new-seller-boost> DESC, COALESCE(bumpedAt,createdAt) DESC, id DESC`; one-tap `BumpListingButton` on seller listings; (6) actions `sponsorSellerAction`/`bumpListingAction` (rate-limited, auth+ownership in service). **Reconciliation:** sponsorship+bump share BOOST_FEE (both promotional placement); the "doesn't stack with boosts" soft rule skipped (wallet cost already deters); instant-fee non-reversal on payout failure documented. All money paths follow guardrails §1/§5 (ledger truth, FOR UPDATE, one tx). FEES.md updated. typecheck + lint (0 errors) + build green | Completes the opt-in monetization layer (Streams 1–7); combined effective take-rate uplift +1.5–3% on GMV with zero base-commission change. Only Stream 8 (ads/affiliate) remains, reserved for Phase 3 |
| 2026-06-10 | **Audit Prompt-16 — anti-fraud architecture (rule-based core)** [AI signals deferred to Prompt 23 / lib/ai.ts]: (1) **Schema** `20260610300000_step16_anti_fraud` — `FraudFlag` (polymorphic targetType+targetId, `@@unique([targetId,reason])`, severity/status/autoAction enums) + `DeviceFingerprint` (`@@unique([userId,fingerprint])`, indexed on fingerprint+ip) + `User.lastLoginIp/lastLoginAt/payoutHeld` + `SellerProfile.payoutHeldAt` + `Order.buyerIp`; 4 new enums; (2) **`config/fraud.ts`** — all thresholds (IP/device/velocity/dispute/review/wash-trade/refund/scam-phrases); (3) **`fraud/risk-score.ts`** — `computeUserRiskScore` + `computeSellerRiskScore` (on-demand from active flags; CRITICAL→100, HIGH+25/MED+10/LOW+3 capped; Step 17 trust subtracts this); (4) **`fraud/signals.ts`** — 12 signals across 6 layers: S1 ip_multi_account, S2 device_multi_account (+HOLD_PAYOUT), S3 order_velocity, S4 dispute-abuse (instant/count/rate + CRITICAL ring escalation), S5 price-anomaly (+FREEZE if also multi-account), S6 high-value, S7 scam-phrase (+FREEZE), S8 review-velocity, S9 review-same-ip, **S10 suspected_wash_trade (CRITICAL: shared IP/device on a PAID order → HOLD_PAYOUT + FREEZE_LISTING atomically)**, S11 micro-order-ring, S12 high-refund-rate (+HOLD_PAYOUT); shared `raiseFlag` upserts on [targetId,reason] (respects admin dismissal), writes AuditLog for HIGH/CRITICAL, runs auto-actions IN the same tx; (5) **`fraud/actions.ts`** — holdPayout/freezeListing/forceReKYC (tx-scoped, reversible); (6) **Integration hooks** (all fire-and-forget via `fireFraudSignal`, never block/crash the main flow): login/load→S1/S2 (via `/api/fingerprint`), createOrder→S3, createListing/updateListing→S5/S6/S7, createReview→S8/S9, openDispute→S4, refund+resolveDispute(buyer)→S12, apply-event post-PAID→S10/S11; (7) **`/api/fingerprint`** route (auth + 5/min rate-limit + Zod) updates lastLoginIp + upserts DeviceFingerprint + fires S1/S2; lightweight dependency-free `FingerprintBeacon` client (SHA-256 of stable signals) mounted in the dashboard layout; (8) **Admin `/admin/fraud`** queue (CRITICAL→HIGH first, severity chips, deep-links) + `dismissFraudFlag`/`actionFraudFlag` actions (ADMIN-gated, one tx, audit-logged; CRITICAL dismiss needs ≥20-char note; dismiss releases auto-actions only if no other severe flag remains) + sidebar link; (9) **Payout guard** — `requestPayout` fails closed when `payoutHeldAt` set; (10) **`scripts/qa-step16.ts`** harness (S1/S2/S7/S10 + upsert + clean-case, marked data). **Reconciliation:** lastLoginIp captured via the fingerprint beacon (Auth.js v5 callback can't see request IP); AI listing/message scam scoring intentionally bundled with the AI layer (Prompt 23) — rule-based S7 scam-phrase covers the top case synchronously now. Guardrails §1/§5/§7 honored; every signal swallows errors. typecheck + lint (0 errors) + build green | A gaming marketplace with real money + no fraud system loses 8–15% of GMV to fraud in 6mo; this layered system (esp. wash-trade CRITICAL + payout-hold) targets 1–3%. Single most important pre-launch risk layer |
| 2026-06-10 | **Audit Prompt-15 — revenue optimization (HIGH-PRIORITY batch: Streams 1/2/4)** [user chose phased scope — Streams 3/5/6/7 queued, 8 is Phase-3 doc-only]: (1) **Schema** `20260610200000_step15_revenue_boosts_pro` — `Listing.isFeatured`+`boostExpiresAt` (+index), `SellerProfile.subscriptionTier`(enum `SellerSubscriptionTier{FREE,PRO}`)+`subscriptionExpiresAt` (+index), `LedgerReason += BOOST_FEE, SUBSCRIPTION_FEE`; (2) **`monetization.ts`** — ONE shared `chargeSellerToPlatform` money primitive (lock seller wallet `FOR UPDATE` → check AVAILABLE balance → DEBIT seller + CREDIT FEE platform, `orderId=null`, both cached balances refreshed) reused by `boostListing` (re-buy extends + stacks time; caps `maxActiveFeaturedPerSeller`; ACTIVE+stock>0 only) and `subscribePro` (30-day extend); `clearListingBoost` for admin recourse; (3) **fees.ts** — `computeBoostFeeMinor(duration)`; `computeSellerCommissionMinorForLevel` extended with `subscriptionTier` (Pro −2pp on top of level discount, floored at 1%, snapshotted on Order.sellerFeeMinor); (4) **marketplace** — `searchListings` now returns `{ items, featured, ... }`; `getFeaturedListings({gameSlug?,type?,minSellerRating?,limit})`; promoted band rendered above organics (MarketplaceResults), on homepage (`FeaturedListingsRail`, 4.0★ gate), and category pages (page 1, by game+kind); **"Promoted" chip is server-set** (RSC prop in the featured row only — never on organic, can't be client-injected); (5) **GETX Pro UI** — `/seller/subscription` pricing page + `SubscribeProButton`, hub upsell/status banner, sidebar link, PRO badge on ListingCard + SellerTrustPanel; (6) **listing cap** reconciled = `max(levelCap, tier===PRO?15:10)`; pause/remove (seller + admin) clear `isFeatured`; (7) **crons** `boost-expiry` (clears lapsed `isFeatured`) + `subscription-renewal` (PRO→FREE past expiry; NO silent auto-charge — trust/insufficient-balance risk) + vercel.json (3am/3:30am UTC, fail-closed Bearer); (8) **actions** `boostListingAction` (10/min) + `subscribeProAction` (5/min) re-check auth, ownership+money enforced in service tx; admin `clearListingBoostAction` (audit-logged). **Reconciliation:** `popular` sort already = best-seller (Prompt 08); commission now layers level (P11) + Pro (P15) discounts in ONE fn. Money rules follow guardrails §1/§5 exactly (ledger truth, FOR UPDATE, one tx). FEES.md updated with pricing + take-rate model. typecheck + lint (0 errors) + build green | Grows take-rate +1.5–3% on GMV with ZERO base-commission change (the USP vs Eldorado/G2G); promoted caps + labels keep the organic feed buyer-trust-first; Pro is self-funding for volume sellers |
| 2026-06-10 | **Audit Prompt-14 — seller conversion (registration → first sale)**: (1) **Schema** `20260610100000_seller_activation_milestones` — `SellerProfile` gains `kycSubmittedAt`/`firstListingAt`/`firstSaleAt` (+3 indexes) and `Wallet.payoutMethodSet`; all set SERVER-SIDE only; (2) **Milestone wiring** (all idempotent `updateMany WHERE xAt IS NULL`): `submitKyc`→kycSubmittedAt, `createListing`(publish)+`setListingStatus`(activate)→firstListingAt, `releaseOrder`→firstSaleAt, first `requestPayout`→payoutMethodSet; (3) **`OnboardingChecklist`** (RSC) on the seller hub — 4 steps (email/KYC/first-listing/payout) with progress bar, collapses to "Shop ready" banner when complete; hub fetches ONE extra `db.user.findUnique` (no dup hit); (4) **`FirstSaleCelebration`** (client, localStorage-dismiss, no schema) — escrow explainer card after firstSaleAt; (5) **Guided first listing** — new `lib/listing-templates.ts` (per-ListingType titlePlaceholder/descriptionTemplate/priceHint/photoTips); `ListingForm` gains `isFirstListing` (photo-tips callout + "Use template" button) + `kycStatus` (non-blocking payout warning near Publish) + price-benchmark helper; new-listing page derives `isFirstListing` from stats; (6) **Escrow explainer** on seller orders page for DELIVERED orders + **`AskForReview`** (copy-paste nudge) on the seller's COMPLETED order view when no review exists (`orderHasReview` helper); (7) **`analytics.ts getSellerActivationFunnel`** (5 parallel indexed COUNTs + conversion rates) wired into `/admin` as a CSS-bar funnel. **Reconciliation:** KYC layout banner ALREADY exists (Prompt 06 AppShell `kycStatus`) — did NOT duplicate; `seller-nav-links` is orphaned (sidebar replaced it Prompt 06) so the nav-dot task is moot (banner + checklist + form callout already make KYC impossible to miss) | Activation funnel was invisible + leaky; checklist + KYC nudges + first-sale explainer are the standard supply-side retention levers (Fiverr/Airbnb/Eldorado). typecheck + lint (0 errors) + build green |
| 2026-06-10 | **Audit Prompt-13 — escrow UX (make protection visible)** (pure trust→conversion; ZERO money/schema/API change): (1) **`EscrowStepper`** (RSC) on the order page — 4-step Payment→Held→Delivered→Confirmed lifecycle, role-aware copy (buyer/seller), past steps check-marked, current highlighted, auto-release deadline note on the delivered step; terminal states (DISPUTED/REFUNDED/CANCELLED/EXPIRED) render a single alert card not a broken progression; `<ol aria-label>` + `aria-current="step"` + sr-only "(completed)"; vertical on mobile, horizontal ≥761px; (2) **`EscrowProtectionPanel`** (RSC, 3 variants) — `full` (3 rows: escrow / money-back / dispute + auto-release footer) on listing sidebar + checkout, `compact` (1 line) replacing the buy-box single LockIcon line, `inline` (slim strip) available; all `siteConfig.escrow.autoReleaseDays`-driven; (3) **`CompactEscrowSteps`** (RSC) — educational 4-step pre-frame at checkout top (desktop-only `hidden min-[761px]:flex`); (4) removed the two redundant `TrustBadge` pills from `SellerTrustPanel` (now covered by the panel above it — kept SellerLevelBadge from Prompt 11); (5) listing delivery section copy now names the dispute path explicitly ("Not happy? Open a dispute — reviewed within 48 hours"); (6) `ConfirmReceipt` countdown reframed from pressure ("auto-releases in X") to protection ("You are protected until X"); dispute turnaround stated (48h). **Reconciliation:** checkout already had `CheckoutTrustBadges` (Prompt 05) serving the inline-reassurance role, so I did NOT also add the `inline` panel there (avoided triple escrow-copy redundancy) | Baymard: ~17% of checkout abandonment is insufficient trust signals — directly addressable. All new components RSC (zero JS bundle delta); only `ConfirmReceipt` (already client) changed, copy-only. typecheck + lint (0 errors) + build green |
| 2026-06-10 | **Audit Prompt-12 — marketplace liquidity & cold-start**: (1) **Schema migration** `20260610000000_step12_liquidity` — `Listing` gains `viewCount`/`lastActivityAt`/`expiresAt`/`newSellerBoostUntil` + 2 indexes (`[gameId,status,newSellerBoostUntil]`, `[status,lastActivityAt]`); new `DemandSignal` model (anonymous email demand capture, unique `[email,categoryId]`) + back-relations on Game/Category; (2) **`liquidity.ts` service** — `getLiquidityStats()` (single `$queryRaw` FILTER-aggregate over all 17 categories, no N+1), `captureWishlistDemand` (validates game+category ACTIVE before upsert → no phantom rows), `bumpListingActivity`, `recordListingView` (fire-and-forget, never throws), `newSellerBoostOrderSql` fragment; (3) **New-seller visibility boost** — `newest` sort (the default) now routes through `searchListingsNewestBoosted`: raw SQL `ORDER BY CASE WHEN newSellerBoostUntil > NOW() AND totalSales < 10 THEN 1 ELSE 0 END DESC, createdAt DESC` selects IDs, Prisma hydrates relations; `buildWhereSql` mirrors `buildWhere` (parameterized, injection-safe; KEEP IN SYNC comment). Boost is display-only — never touches money/escrow/fees; `createListing` sets `newSellerBoostUntil = +7d` when `totalSales < 10`, `expiresAt = +60d`; (4) **Stale-pause cron** `/api/cron/auto-pause-stale` (daily 3am UTC, Bearer fail-closed, `updateMany`+`createMany` notifications+audit in ONE tx, idempotent via `status:ACTIVE`); (5) **Demand capture UI** — `DemandCaptureCard` client island (`captureDemandAction` server action: Zod email + IP rate-limit 5/hr) replaces silent empty-category drops on the game landing (compact) + category page (full); `MarketplaceResults` game-filtered empty state points to the game page (per-category demand lives there); `CategoryCard` gains `hasSupply` (BellIcon + "Request a seller" when 0); (6) **Admin `/admin/liquidity`** (ADMIN-gated, `revalidate=60`) — supply-health table, red/amber/green rows, fill-rate bars, summary tiles + sidebar nav link; (7) `siteConfig.liquidity` (`staleListingDays:60`, `newSellerBoostDays:7`, `newSellerBoostMaxSales:10`); (8) **Seed** expanded 4→12 listings (8 in Pokémon GO — all 4 categories filled) + 3rd "newcomer" seller (totalSales=3) demonstrating the boost; activity bumps wired into `updateListing`/`setListingStatus(activate)`. **Reconciliation:** Step 8 `best_seller` sort = the existing `popular` sort (Prompt 08, totalSales DESC) — added `best_seller` as a parseable URL alias (same orderBy) rather than a duplicate dropdown entry. **Deploy note:** migration applies on a Neon branch via `prisma migrate deploy` (NOT main until QA) | Liquidity = the #1 marketplace killer; demand capture converts bounces to leads, new-seller boost retains first-time sellers, auto-pause prevents ghost-seller disputes, admin dashboard turns "we feel thin" into "PUBG has 0 supply vs N demand — recruit here". typecheck + lint (0 errors) + build green |
| 2026-06-09 | **Audit Prompt-11 — trust engine + seller levels**: (1) **DB migration** `20260609150000_step11_trust_reputation` adds 7 fields to `SellerProfile`: `trustScoreBreakdown` (JSONB), `trustScoreUpdatedAt`, `trustScoreOverride` (boolean — locks nightly cron override), `sellerLevel` (String `BRONZE`..`ELITE` — not an enum so thresholds can change without migration), `sellerLevelUpdatedAt`, `riskScore` (0-100 scam sub-score for Fraud Radar), `riskScoreUpdatedAt`; two indexes on `sellerLevel` + `riskScore`; (2) **`trust-score.ts` service** — canonical 6-signal formula (completionRate 30pts + ratingScore 25pts + responseTime 20pts + accountAge 10pts + kycVerified 10pts + disputePenalty -15max = 0-100); `computeRiskScore` (scam sub-score feeds Prompt-18 Fraud Radar); `resolveSellerLevel` walks ELITE→BRONZE with 4 criteria per level; `recomputeSellerTrustAndLevel` — DB-writing fire-and-forget, NEVER inside a tx; (3) **5-level perks** fully configured in `SELLER_LEVELS[]`: commission discounts BRONZE 0% → ELITE 5%, listing caps 10→500, payout speed 3→1 day, searchBoostFactor 1.0→1.35; (4) **Hooks**: `createReview`/`editReview` (reviews.ts), `confirmReceipt` (escrow.ts), `runAutoRelease` (batch, deduped by seller), `reviewKyc` (only on APPROVE) — all capture `sellerProfileId` outside tx, fire-and-forget post-commit; (5) **`trust-broadcast.ts`** — POST to socket server `/internal/trust-updated` (fail-graceful, 3s timeout, no-op if `SOCKET_INTERNAL_URL` unset); (6) **Level-discounted commission** in `fees.ts` (`effectiveSellerCommissionPct` + `computeSellerCommissionMinorForLevel`); `orders.ts` `createOrder` now selects `sellerLevel` and uses level-discounted rate (snapshotted on Order.sellerFeeMinor as before); (7) **Listing cap** enforced in `createListing` — checks `ACTIVE` count inside tx against level cap before publish; safe for draft-saves (publish-only gate); (8) **Display components**: `SellerLevelBadge`, `TrustScorePill`, `LevelProgressPanel` (progress to next level with 4 requirement rows); wired into `SellerTrustPanel`, `ListingCardData`, `listingCardInclude`, `ListingDetail`, `SellerPublicProfile`; (9) **Nightly cron** `/api/cron/trust-score` (02:00 UTC, `trustScoreOverride=false` only, 200/run batch, auth fail-closed); (10) **Admin override** `overrideTrustScoreAction` + validator — hard-sets score+level and flips `trustScoreOverride=true`, AuditLog entry; (11) `SOCKET_INTERNAL_URL` added to `.env.example` | `sellerLevel` as String (not enum) = threshold changes without migration; recompute is ALWAYS post-commit fire-and-forget (never inside a tx) to avoid tx bloat on the escrow/review hot path. typecheck (0 errors) + lint (0 errors) + build green |

| 2026-06-09 | **Audit Prompt-10 — performance**: (1) **GSAP bundle confirmed scoped to `(marketing)` shell only** (CinematicFooter is ONLY in `src/app/(marketing)/layout.tsx`; shop/dashboard/checkout never pay the GSAP cost — ~30 KB min+gz eliminated from non-marketing routes); `framer-motion` is installed but unused in `src/` (zero imports found); (2) **`getCategoryPreviews` batched from N parallel queries → 1** (was `Promise.all(stocked.map(...findMany))` — up to 4 Neon round-trips for a game with 4 categories; replaced with a single `findMany` where `categoryId IN [...]`, grouped in memory — 1 round-trip always; correctness: `take: perCategory * stocked.length` + in-memory group cap); (3) **ISR `revalidate` exports added**: homepage 3600s (game tiles are stable), `/games` + `/games/[slug]` + `/games/[slug]/[category]` 300s (listing counts tolerate 5 min staleness), `/listing/[slug]` 60s (price/stock can change); marketplace page intentionally excluded (searchParams-driven, dynamic by design — comment added); (4) **`priority` prop on above-the-fold listing images**: first category's first 2 cards on game landing pages (`catIndex === 0 && cardIndex < 2`), first 4 cards on marketplace (`i < 4`) and category pages (`i < 4`) — already correct on category + marketplace; (5) **`@next/bundle-analyzer` added** as devDependency + `npm run analyze` script (ANALYZE=true gate, Vercel build MUST NOT set ANALYZE=true); JSDoc added to `r2RemotePatterns` documenting the CLS-risk fallback when R2_PUBLIC_BASE_URL is missing at build time; (6) **pre-existing `listing-form.tsx` lint fix**: `watch()` → `useWatch({control,name})`, `submit` → `useCallback`, form handlers → `useMemo` outside JSX (`react-hooks/refs` false-positive on event handlers suppressed with inline disable comments) | ISR → CDN edge cache; Neon query reduction (~4→1 per game landing render); `priority` on LCP candidates prevents lazy-loaded images causing Core Web Vitals failures; bundle analyzer gated so CI builds are unaffected |
| 2026-06-10 | **Step 22 — Notification system (in-app bell + Resend email + Socket.io push)** (built ahead of roadmap 16–21; code deps 09–15 all done): (1) **Schema** migration `20260610500000_step22_notifications` — `Notification` gains `read Boolean`, `link String?`, indexes `[userId,read]` + `[userId,createdAt]`, `title`/`body` bounded to VarChar(80/200), and `type` String→**`NotificationType` enum** (ORDER_UPDATE/NEW_MESSAGE/DISPUTE/PAYOUT/REVIEW/SYSTEM) via an in-place USING cast (kept existing `readAt` + its index — additive, zero drops); `User.emailNotifications Boolean @default(true)`; (2) **Service** `notifications.ts` — `createNotification` (clamps length, open-redirect-safe relative-`link` guard, fire-and-forget socket push, never throws), `sendNotificationEmail` (email preference read from the DB not the caller, no-op when `RESEND_API_KEY` unset), 6 **self-resolving** trigger helpers (`notifyOrderEvent`/`notifyDisputeEvent`/`notifyNewMessage`/`notifyPayoutEvent`/`notifyNewReview`/`notifyKycDecision` — resolve audience from the order/payout id so callers add ONE line, no select expansion in money services), read-side (`getNotifications`/`countUnreadNotifications`/`markNotificationRead` ownership-scoped via WHERE/`markAllNotificationsRead`); (3) **Triggers** wired fire-and-forget POST-commit (never awaited on a money path): payment PAID (`apply-event.ts`), escrow DELIVERED/COMPLETED/REFUNDED + dispute OPENED(+admins)/RESOLVED + auto-release, payouts PAID/FAILED, KYC APPROVE/REJECT, new review, new chat message (recipient only, sender excluded); (4) **Socket** — direction is APP→socket-server (the inverse of chat): `index.ts` captures `io` + adds an authenticated `POST /notify` (timing-safe Bearer `INTERNAL_API_SECRET`, fail-closed) → `io.to('user:<id>').emit('notification:new')`; `server.ts` joins each socket to its `user:<id>` room on connect; app pushes via `src/lib/socket-notify.ts` (`SOCKET_INTERNAL_URL`, 3s timeout, fail-open) + a thin `/api/internal/notify` route; no new env var (reused Step-11 secrets); (5) **UI** — `NotificationBell` (client, self-contained popover, realtime dedupe by id, optimistic mark-read, mobile-safe `w-80 max-w-[calc(100vw-1rem)]` + `max-h-[60vh]`) mounted in `site-header` with server-fetched initial unread+list; reusable `useSocket` hook; `/settings/notifications` email toggle; (6) **Email** — `resend.ts` lazy null-safe singleton, 6 inline-CSS v10-branded templates (HTML-escaped, absolute links), `RESEND_*` in `.env(.example)`; `mail.ts` verification/reset now go live via Resend (console fallback when unkeyed). **Decisions:** kept `readAt` AND added a `read` boolean (O(1) unread count for the badge); single global email toggle for MVP (per-category later); email preference always enforced server-side from the DB. qa-step22.ts **42/42** + typecheck/lint/app-build/socket-build green | Notifications = the marketplace's nervous system: seller response time (the niche differentiator) drops from hours to minutes, buyer confirm-rate rises (faster escrow release), dispute rate falls (most disputes are comms failures). Every notification is a pure fire-and-forget side-effect so a failure can NEVER roll back or slow a money/order transaction; emails no-op gracefully until the owner adds `RESEND_API_KEY` |
| 2026-06-10 | **Audit Prompt-19 — founder analytics cockpit (`/admin/analytics`)**: read-only KPI cockpit, ADMIN-gated. (1) **`founder-analytics.ts`** — 7 aggregates: revenue+GMV trend (gap-filled daily, raw SQL `DATE_TRUNC`), take-rate series (pure transform, zero DB), order funnel (single `groupBy status`), trust health (8 parallel counts + distinct-active-seller raw SQL), top-5 games by revenue + revenue-by-category (join `LedgerEntry→Order→Listing→Game`, `COUNT(DISTINCT o.id)` to avoid fee-row fan-out double-count), new-seller monthly activation cohorts (`LATERAL` first-sale subquery). **Revenue truth = `LedgerEntry` platform-wallet FEE/CREDIT rows, never `Order.feeMinor`** (orders refund after ledger). All money minor units; BigInt aggregates → Number before return (JSON-serializable for cache). (2) **Caching:** each aggregate = a plain `*Impl` + an `unstable_cache(impl, key, {revalidate:300})` export; raw impls re-exported via `_analyticsImpl` for the QA harness (unstable_cache throws "incrementalCache missing" outside a Next request). (3) **Page** `/admin/analytics` — `requireRole("ADMIN")` (defense-in-depth, not layout-only), `?days=7|30|90` Zod-validated (`.catch("30")`), 7 sections, `Promise.all` parallel fetch + route `loading.tsx`, mobile grids + `overflow-x-auto` tables. (4) **Chart** = CSS/Tailwind dual-bar (no Recharts installed — table/bar fallback per spec). (5) **4 indexes** migration `20260610600000_step19_analytics_indexes` (LedgerEntry `[walletId,reason,type,createdAt]`, Order `[status,updatedAt]` + `[listingId,status,updatedAt]`, SellerProfile `[kycStatus,createdAt]`). **Reconciliation:** reused existing `analytics.ts::getSellerActivationFunnel` (indexed milestone cols) for the seller-funnel section instead of duplicating; inline `<Link>` day-range buttons (server-rendered) instead of a client picker; nav link added to AppShell admin sidebar after "Control room". qa-step19.ts **32/32** + typecheck/lint/build green | A founder flying blind on a two-sided marketplace is a top growth-stage failure mode; GMV/take-rate trend + dispute-rate early-warning + seller funnel drop-off tell the founder where to invest next. All raw SQL is parameterized (`$queryRaw` tagged templates), read-only, ADMIN-gated, no PII |
| 2026-06-10 | **Audit Prompt-20 — "Why GETX Wins" strategy doc** (`docs/WHY-GETX-WINS.md`, doc-only — no code commit per spec): a living, code-grounded competitive-moat playbook. **Corrected the original audit's now-stale state** (it predates prompts 11–19): trustScore + 5-tier levels, notifications, fraud graph, revenue streams, liquidity, SEO, founder analytics are ALL now built and verified in-code — so the formerly "#1 preventable" kill-criterion (#10 invisible trust) is CLOSED. Remaining honest gaps documented: liquidity/GTM execution (top risk), auto-delivery engine (`DeliveryType.INSTANT` enum but no service), and the AI layer (Claude wired in stack-decision but called nowhere in `src/` — the open moat, Prompt 23 next). Includes: updated comparison matrix, 4-layer moat status, survival-ordered Phase-2 sequencing, GTM pre-seed plan, top-10 kill criteria with current mitigation state, and §9 the one un-built GTM mechanic (capped `isFoundingSeller` zero-commission flag — flagged as a money-path build, not done) | Strategy is the document that decides whether every other prompt is worth executing; keeping it code-grounded + current prevents building features nobody uses and prevents fake-metric outreach. QA = traceability (every "built" claim → a real file) rather than typecheck |
| 2026-06-10 | **Audit Prompt-21 — community & network-effects design spec** (`docs/COMMUNITY-DESIGN.md`, design-only — "No code is written here" per spec): the buildable blueprint for the stickiness moat, 5 phases ROI-ordered (Follow + buyer UserProfile/handle → Wishlist + Collections → Guides CMS → Leaderboards → Forums/Clans), each with Prisma models (idempotent `@@unique` toggles), engagement loop, LIVE notification hook, abuse controls, and the network effect it creates. **Updated for current state:** the two hardest deps are now built — trust score + levels (Prompt 11) and the notification system (Step 22) — so every "notification hook" (new-listing-from-followed-seller fan-out, wishlist price-drop/restock re-engagement) is a real `notify*()` call today, and `Listing.viewCount/lastActivityAt` (Prompt 12) already exist for demand signals. Flagged Phases 1/2/4 (Follow, Wishlist, Leaderboard) as bounded + ready-to-build; Phases 3/5 (CMS, forums) as larger + gated on moderation tooling | The reputation graph is the data moat; community is the stickiness moat ("I live here" vs "I shop here"). Design-spec deliverable (not code) faithful to the prompt; correcting stale deps means a dev can execute Phase 1 immediately on the live notification system |
| 2026-06-10 | **Audit Prompt-22 — referral & viral growth (fraud-safe core, implemented)**: double-sided buyer referral. (1) **Schema** migration `20260610700000_prompt22_referral` — `Referral` model (`refereeId @unique` = referred by ≤1 person; `bonusAwarded` CAS guard), `User.referralCode @unique` + `referredBy` + `referralCreditMinor`, `NotificationType.REFERRAL`, referral enums. (2) **Reward currency = FEE_CREDIT fallback** (`User.referralCreditMinor` paise) since Step 21 loyalty points aren't built — the prompt's own degradation path; `src/config/referral.ts` is the rate SoT (referee ₹25 signup credit, referrer ₹50 buyer / ₹125 seller). (3) **`referral.ts` service** — `ensureReferralCode` (8-char A-Z0-9, collision-retry, idempotent), `attributeReferralAtSignup` (PENDING referral + referee signup credit; **self-referral + unknown-code + already-referred guards**; NEVER throws so signup can't break), `checkAndAwardReferralBonus` (**deferred** referrer reward via `bonusAwarded` CAS flip → credit increment gated by `count===1` → idempotent under concurrent completions; never throws), `getReferralStats` (privacy-masked history). (4) **Escrow integration** = POST-COMMIT fire-and-forget `void checkAndAwardReferralBonus(buyerId)` in `confirmReceipt` + `runAutoRelease` (NOT inside releaseOrder's tx — safer than the prompt's literal "inside tx" suggestion; CAS still guarantees once; can never block/rollback seller payment). (5) **Signup** `?ref=` → register page → form hidden field → `registerSchema.ref` → `registerAction` → attribution. (6) **UI** `/referrals` dashboard (share link + copy/Web-Share, credit, history) + sidebar nav. **Adversarial review:** idempotency/self-referral/already-referred/signup-safety/escrow-safety all CLEAN; 1 fix applied (referee-name privacy leak in history → masked to 3 chars since codes can be public). qa-referral.ts **20/20** + typecheck/lint/build green. **Deferred (noted):** affiliate/streamer layer (AffiliateCode/AffiliateConversion), K-factor admin dashboard, checkout redemption of referralCreditMinor, separate seller-invite UI — follow-ups | Referral is the primary acquisition channel for the first 1,000 users (Dropbox/Revolut precedent); deferred reward at the genuine conversion event (escrow release) is the fraud gate that makes signup-farming worthless. Reward accrual uses the same CAS-idempotency rigor as the money paths |
| 2026-06-10 | **Audit Prompt-23 — unified AI layer (`src/lib/ai.ts`, built) + AI-native architecture** (`docs/AI-LAYER.md`): ONE coherent Claude client every future AI feature consumes, ending the per-step re-instantiation + re-written JSON parsing the step prompts each specified. (1) **`ai.ts`** — `getAnthropic()` lazy null-safe singleton (null when `ANTHROPIC_API_KEY` unset), `isAiEnabled()`, `generateText()` and `generateJSON<T>(zodSchema)` (Claude → extract-JSON → Zod-validate, 1 retry, typed-or-null), `AI_MODELS` map per CLAUDE.md §3 (default `claude-sonnet-4-6` / reasoning `claude-opus-4-8` / fast `claude-haiku-4-5`). Official `@anthropic-ai/sdk` (per CLAUDE.md §3 + claude-api skill — not the Vercel AI-SDK/gateway). (2) **Operating principles enforced by design:** AI suggests, rules/humans decide money (helpers can't touch the ledger/escrow); env-safe degradation (no key → every helper returns null/fallback, never crashes — same pattern as Resend); helpers NEVER throw (so an AI outage can't break a page or money path) and are fire-and-forget for non-critical AI. (3) **`AI-LAYER.md`** = the funnel-wide feature map (listing drafter/buyer concierge/trust explainer/AI support/**Dispute Judge** Opus+pgvector/fraud scorer/AI pricing — each a future step consuming the layer) + how each plugs in money-safely. **Robustness choice:** `generateJSON` uses create + extract + Zod-safeParse (works on any SDK version + model) rather than `messages.parse`/`output_config.format`, so the foundational layer isn't coupled to a specific SDK/model capability. qa-ai.ts **9/9** (degradation contract: unkeyed → null/no-throw; keyed gating; model map) + typecheck/lint/build green. **To go live:** owner adds `ANTHROPIC_API_KEY` | The AI-native funnel is GETX's primary defensible moat (incumbents are manual-form marketplaces + legacy infra = 12-month rebuild to copy); one clean layer means each AI feature ships in weeks on the existing stack, and the never-throw + money-safe design means AI can fail without ever touching trust or money |
| 2026-06-10 | **Audit Prompt-24 — marketplace operations (SLA work-queue core, implemented)**: unified ops queue so GETX scales past a solo-admin clicking flat lists. (1) **Schema** migration `20260610800000_prompt24_work_tickets` — `WorkTicket` (type/priority/status enums, soft `entityType`+`entityId` ref, `slaDeadlineAt`, `firstResponseAt`, `assignedToId`, `resolvedAt`/`closedAt`, 4 indexes incl. `[slaDeadlineAt,status]` for the breach sweep) + `TicketNote`; `TicketType`/`TicketPriority`/`TicketStatus` enums. (2) **`work-queue.ts` service** — `computeSlaDeadline` (createdAt + `siteConfig.ops.slaHours[type]`), `createTicket` (**idempotent**: returns the existing open ticket for an entity, so a retried hook never duplicates), `assignTicket`/`addNote` (stamp `firstResponseAt` on first agent touch), `closeTicket`, `listOpenTickets` (CRITICAL→HIGH→NORMAL→LOW then `slaDeadlineAt ASC` + `slaBreached` flag), `getOpsMetrics` (queueDepth/SLA-attainment/median-resolution via `PERCENTILE_CONT`/auto-resolution/breachedOpen/agentLoad/highPriorityOpen), `sweepSlaBreaches` (**idempotent** — escalates breached non-CRITICAL → CRITICAL + `SLA_BREACHED` audit once, since CRITICAL is then excluded). (3) **Triggers**: fire-and-forget post-commit `ticketForDispute` in `escrow.openDispute` (HIGH if order ≥ ₹50k) + `ticketForKyc` in `kyc.submitKyc` — money services unaffected. (4) **Cron** `/api/cron/sla-breach` (fail-closed Bearer `CRON_SECRET`, `*/30` in vercel.json). (5) **`/admin/ops`** dashboard (queue-by-type + breach badges, ops-health strip, prioritized queue, agent load) + sidebar nav. **Reconciliation (deferred + noted):** RBAC role expansion (SUPPORT/MODERATOR/FRAUD_ANALYST/FINANCE — a broad auth-enum change, kept out to not touch the Role enum/session/middleware), fraud + payout ticket sources, AI deflection (consumes the Prompt-23 `ai.ts` layer), and the user-facing ticket detail/assignment UI — follow-ups. Built a separate `/admin/ops` rather than rewriting the admin home (lower risk). Dropped the `WorkTicket↔AuditLog` relation (audit via action+entityId, no FK churn). qa-ops.ts **24/24** + typecheck/lint/build green | The ops gap is a growth ceiling (a solo admin collapses at ~500 orders/day); SLA tracking + a prioritized queue + breach escalation make dispute/KYC throughput auditable and bounded. Ticket creation is fire-and-forget so a queue failure can never block a dispute/KYC; all money mutations stay in escrow/payout |
| 2026-06-10 | **Audit Prompt-25 — scale audit (`docs/SCALE-AUDIT.md`, analysis-only — no code change per spec)**: code-grounded "what breaks FIRST at 100k/500k/1M," each with file:pattern + fix + trigger metric. **Tier 1 (~100k):** ILIKE search (`marketplace.ts` leading-`%` = seq scan → pg_trgm GIN then Algolia/Step 28), offset pagination + live `count` (→ keyset), in-memory per-lambda rate-limit Map (effective limit = N×limit → Upstash Redis/Step 32). **Tier 2 (~500k):** `getWalletBalances` full-ledger `groupBy` every read inside the payout lock (→ authoritative running balance), auto-release single-sweep 200/run×`*/15` = 800/hr ceiling (→ durable queue), single Socket.io process holding rooms+buckets in memory (→ Redis adapter + horizontal), lossy fire-and-forget side-effects (→ durable queue, at-least-once). **Tier 3 (~1M):** `PLATFORM_WALLET_ID="platform"` single-row write hotspot (→ drop hot-path cached balance / shard), unbounded append tables (→ time-partition + archival + read replica), live analytics aggregates (→ materialized rollups behind the already-cache-isolated cockpit fns), Neon pool saturation (→ read replica). Phased P0–P3 roadmap mapped to Steps 28/32/33 + the pieces they miss (durable queue, socket horizontal scaling, read replicas, rollups, partitioning, platform-wallet contention). **Principle:** stay a modular monolith past 1M, extract only realtime (done) + worker tiers | Knowing the exact first-failure line + trigger metric per tier turns "the site got slow" panic into a pre-agreed sequence; prevents both premature over-engineering (runway burn) and being blindsided at the growth curve |

| 2026-06-10 | **Step 19 — Auto / instant delivery (built)**: zero-wait fulfilment for digital goods. (1) **Schema** migration `20260610900000_step19_delivery_items` — `DeliveryItem` (content AES-256-GCM encrypted at rest, `status` AVAILABLE/RESERVED/DELIVERED, `orderId?`, indexes `[listingId,status]` + `[orderId]`); seller→SellerProfile (matches Listing.sellerId — reconciles the prompt's `User` ref). (2) **`encryption.ts`** — AES-256-GCM, **fail-closed**: missing/malformed `DELIVERY_ENCRYPTION_KEY` (64 hex) throws immediately, never stores plaintext; `isEncryptionAvailable()` gates the UI; key never logged. (3) **`delivery.ts`** — addDeliveryItems (encrypt + bulk insert, owner+INSTANT guard, auto-unpause on refill), deleteDeliveryItem (AVAILABLE-only), getSellerDeliveryStock (masked first-4-char preview), **autoDeliver** (own short tx, `FOR UPDATE SKIP LOCKED` picks ONE item + CAS order PAID→DELIVERED + autoReleaseAt; idempotent: no-ops if not-PAID/already-delivered), pauseListingOnStockout, **getDeliveryContentForOrder** (decrypt-on-read, buyer/seller/admin-gated — plaintext never stored/sent-encrypted). (4) **Payment wiring** — auto-delivery runs **POST-COMMIT in its own tx** (NOT inside applyPaymentEvent's tx) so it never contends with the seller-wallet escrow `FOR UPDATE` lock; awaited so the item is ready before the webhook returns; stockout/no-key → `DeliveryStockoutError` → order stays PAID (MANUAL fallback) + LOW FraudFlag `auto_delivery_stockout` + buyer-DELIVERED-vs-PAID notification branch. (5) **UI** — seller `DeliveryStockManager` on listing edit (upload/stock-badge/low-stock alert/masked delete; hidden + neutral banner when key absent), buyer `DeliveryContentCard` (decrypted + copy), ⚡ badge on cards + buy-box already present. **Key design reconciliations:** prompt self-contradicted ("never store plaintext" vs "create OrderDelivery with decrypted plaintext") → resolved in favour of security (content encrypted at rest, decrypt-on-read, no plaintext OrderDelivery row); moved auto-delivery out of the payment tx (a 5s interactive-timeout under serialized escrow locks proved the in-tx approach contends) — separate DeliveryItem lock domain keeps concurrency safe + fast. **Key rotation** = re-encrypt all `DeliveryItem.content` in a maintenance window (out of MVP scope). qa-step19-delivery.ts **27/27** incl. the concurrency proof (2 items, 3 buyers racing → exactly 2 delivered to DISTINCT orders, 1 MANUAL fallback, all 3 payments succeed) + typecheck/lint/build green. **To go live:** owner sets `DELIVERY_ENCRYPTION_KEY` (`openssl rand -hex 32`) | Instant delivery is table-stakes (Eldorado/G2G hand over codes in seconds; a buyer won't wait for manual typing even to save 2%). The fail-closed crypto + payment-never-fails-on-stockout design means the feature can't leak secrets or lose a buyer's money to a seller's stock mistake |
| 2026-06-10 | **Step 16 — AI Support bot (built)**: a 24/7 streaming support chat that consumes the Prompt-23 `ai.ts` layer. (1) **Schema** migration `20260610950000_step16_support_ticket` — `SupportTicket` (`userId?` nullable for guests + `onDelete: SetNull`, `subject`, `chatHistory` Json transcript, `status` OPEN/CLOSED, `adminNote`, indexes `[status,createdAt]` + `[userId,status]`); kept DISTINCT from the Prompt-24 ops `WorkTicket` (whose `entityType` already anticipated `"SupportTicket"`) — this stores the raw buyer/guest transcript an agent reads. (2) **`ai.ts` += `streamSupportResponse()`** — async-generator of text deltas via `client.messages.stream`; UNLIKE generateText/JSON it does NOT swallow upstream errors (the route needs to flush an error SSE), yields nothing when AI disabled. (3) **`support-prompt.ts`** — `buildSystemPrompt(context?)` with HARDCODED policy facts (escrow lifecycle, buyer 5% + seller tiers pulled from `siteConfig`, dispute 48h SLA, payout T+2/₹500, no-emoji, escalation instruction "include 'I don't know'"); live per-user data injected separately, never from the prompt file. (4) **`support.ts` service** — `getSupportContext` (server-side: last 5 non-DRAFT orders + open disputes; returns "" on error, NEVER throws), `detectEscalation` (pure: user asks for human regex OR AI low-confidence phrase), `createSupportTicket` (**idempotent** per userId+subject — refreshes the existing OPEN ticket instead of duplicating; guests always get a fresh row), `closeSupportTicket` (CAS + audit in one tx, **idempotent** — re-close writes no dup `CLOSE_SUPPORT_TICKET` audit), list/get read-models + `parseChatHistory`. (5) **Route** `POST /api/support/chat` (SSE) — guard order: `isAiEnabled()` 503 → rate-limit 30/hr (userId or IP) 429 → Zod 400 (content ≤500, ≤20 turns); context injected server-side from the session (client identity NEVER trusted); escalation after the full reply files a ticket + flushes `{done,escalated}`. (6) **Widget** `support-widget.tsx` (client) — floating launcher + drawer (mobile sheet / desktop panel), fetch+ReadableStream SSE rendering, typing dots, 20-turn reset banner, escalation banner, char counter; gated by `NEXT_PUBLIC_SUPPORT_ENABLED`, **self-hides on 503**; mounted in shop + dashboard layouts (guests welcome). (7) **Admin** `/admin/support` queue (All/Open/Closed tabs) + `/admin/support/[id]` transcript + close form (ADMIN-gated action, audited) + sidebar link. **Design choice:** QA is in-process (`qa-step16-support.ts`, separate from the audit-Prompt-16 anti-fraud `qa-step16.ts`) because the dev server has no key — the route's env-gated 503 can't be toggled from an external HTTP client; the service/validator/prompt/escalation layers are driven directly. **AI never moves money** (only reads context + files tickets). qa-step16-support.ts **47/47** + typecheck/lint/build green. **To go live:** owner sets `ANTHROPIC_API_KEY` (widget activates, no code change) | 24/7 instant support is a trust + conversion lever (a buyer mid-checkout with a question who gets an answer in seconds doesn't bounce); guest support is a deliberate selling point. The never-throw + 503-self-hide + money-isolated design means an AI outage degrades to "widget hidden," never a broken page or a wrong payout |
| 2026-06-10 | **Step 17 verification — found + fixed a latent bug**: `recomputeSellerTrustAndLevel`'s reply-time `$queryRaw` referenced `c.created_at` / `m.created_at` (snake_case) but the columns are camelCase (`"createdAt"`), and the outer query referenced an out-of-scope alias `c`. The query threw `42703 column does not exist` on EVERY recompute — so the avg-first-reply signal silently failed and (since recompute had no inner try/catch) the whole trust/level/risk write was skipped on review/escrow/kyc hooks + the nightly cron. Fixed the query (quoted camelCase columns + a correct single-subquery AVG over the seller's first reply per conversation, `ORDER BY conv_created DESC LIMIT 20`). Added `scripts/qa-step17-trust.ts` (23/23) incl. a 10-min-reply case proving `responseTime` now computes to 20 | A feature marked "done" was silently broken in production; the user's "verify" instinct caught it. Lesson: a raw-SQL signal with no inner error handling can fail-open and freeze a whole derived score without anyone noticing — verification harnesses on "done" features pay for themselves |
| 2026-06-10 | **Step 20 — Seller "CEO" dashboard (built)**: `/seller/analytics` cockpit. (1) **`seller-analytics.ts`** — 5 read-only `React.cache()`-wrapped fns, integer minor units: `getRevenueSeries` (raw `DATE()` group + JS gap-fill so the chart is continuous), `getTopListings` (top-5 by completed count, revenue = Σ SALE), `getOrderFunnel` (Prisma groupBy, zero-filled stages), `getWalletSummary` (available/held via `getWalletBalances`), `getPriceBenchmark` (peer avg/min/max, null when no peers). **RECONCILED to the real money model:** the prompt assumed a `userId`+`kind='SALE'/'COMMISSION'` ledger, but ours is **wallet-based** — seller revenue = CREDIT/SALE on the seller's Wallet (already net of commission), and commission paid = the `order.sellerFeeMinor` snapshot (platform FEE rows live on the PLATFORM wallet, indistinguishable per-seller). No new index needed — existing `LedgerEntry[walletId, reason, type, createdAt]` covers the revenue query. (2) **`getAIPricingSuggestion` action** (Haiku via `ai.ts`) — re-auth + listing-ownership guard, grounds Claude in price + peer benchmark + 30-day sales, returns one sentence + range; graceful fallback string when no key / on error (never 500); nothing persisted (AI suggests, seller decides). (3) **UI** — Recharts **v3** (installed; React-19 native) LineChart + BarChart in `ResponsiveContainer` + `overflow-x-auto min-w` for mobile; client charts, server data; 4 stat cards (2×2 mobile→4 desktop); `DateRangePicker` 7/30/90; `TopListingsTable` with per-row AI "Suggest" button (`useTransition` spinner, inline result); each section in its own `<Suspense>` with a skeleton. Built at `/seller/analytics` (the sidebar already had it as "soon" → un-soon'd) rather than the prompt's `/seller/dashboard`, since `/seller` is already the overview. Recharts v3 gotcha: custom tooltip must be typed `TooltipContentProps` (not `TooltipProps`) and passed to `content={Fn}` as a function, not a JSX element. qa-step20.ts **26/26** + typecheck/lint/build green. **To go live (AI button):** owner sets `ANTHROPIC_API_KEY` | Sellers-as-CEOs is a core strategy pillar (make sellers feel like they run a business); a revenue/funnel/top-listings cockpit + AI price coaching is the retention hook. Reconciling to the real wallet ledger (not the prompt's imagined schema) keeps the numbers truthful to the money system |
| 2026-06-10 | **Step 21 — Loyalty points (built); referral half left as-is**: the step's referral spec was ALREADY shipped (Audit Prompt-22, FEE_CREDIT reward + CAS bonus at release) — so this pass builds ONLY loyalty points and does NOT add referral *points* (no double-reward). (1) **Schema** migration `20260610970000_step21_loyalty_points` — append-only `LoyaltyPoint` (amount always positive, direction in `type` EARN/REDEEM; reasons SIGNUP_BONUS/PURCHASE/SALE/REDEMPTION/PURCHASE_REFUND; `@@unique([userId, orderId, reason, type])` makes order-bound awards/redemptions idempotent) + `Order.loyaltyPointsRedeemed` snapshot. Balance is ALWAYS derived (ΣEARN−ΣREDEEM) — never a stored total (same rule as the money ledger). (2) **`config/loyalty.ts`** = rate SoT (buyer 1pt/₹10, seller 1pt/₹20, 50 signup, 1pt=10 paise, 20% cap) + pure helpers. **`loyalty.ts` service** — `getLoyaltyBalance(tx-aware)`, `awardPoints` (createMany+skipDuplicates → idempotent, NEVER throws inside a money tx), `redeemPoints` (balance-guarded), `awardSignupBonus` (idempotent, never throws), `computeRedemptionCap`. (3) **CRITICAL safe-redemption design:** a redeemed discount reduces BOTH `order.totalMinor` AND `order.feeMinor` (the buyer platform fee) by the same amount, capped at `D ≤ feeMinor`. Result: the existing escrow reconciliation `saleMinor + feeMinor + commission === totalMinor` HOLDS UNCHANGED (both sides drop by D) — so `releaseOrder` needed **zero** reconciliation surgery, the seller's take is never touched, and the discount is fully platform-funded. (The prompt's literal "deduct from subtotal" would have broken the release reconciliation + cost the seller money.) (4) **Integration** — signup bonus in `registerAction`; earn (buyer PURCHASE on subtotal + seller SALE on net) inside `releaseOrder`'s tx (idempotent); redemption in `createOrder` (server re-clamps the advisory client value to balance ∧ 20% ∧ platform-fee, redeems in the same tx; reusing an open order preserves its prior redemption); refund reversal in `refundInTx` (compensating PURCHASE_REFUND EARN restores redeemed points — ledger stays balanced, nothing deleted). (5) **UI** — `/loyalty` (balance + redeem explainer + refer link + history) + `/seller/loyalty` (SALE-filtered) + a checkout "use N reward points" toggle (server re-clamps regardless) + sidebar nav. qa-step21.ts **23/23** drives the REAL full money cycle (buy→redeem→pay→deliver→release→refund) and asserts: release does NOT throw with a discount applied, seller SALE credit = full saleMinor, refund restores points, idempotency holds. typecheck/lint/build green; regressions qa-step08 24/24 + qa-step19 27/27 | Loyalty is the repeat-purchase retention loop ("earn on every buy/sell, redeem as discount"). Funding the discount out of the platform fee (capped at the fee) is the one design that keeps the seller whole AND avoids rewriting the audited escrow reconciliation — safety over the prompt's literal money model |

| 2026-06-10 | **Step 24 — Mobile-first PWA (built)**: GETX is now installable. (1) `public/manifest.json` (standalone, portrait, `#0a0b0d` bg / `#4d7cfe` theme, 96/192/512 icons + 2 screenshots + shortcuts). (2) **Hand-written `public/sw.js`** using Workbox 7.3.0 via CDN `importScripts` — **NOT `next-pwa`** (known incompatibility with Next 16 App Router / Turbopack). Strategies: static `/_next/static` + fonts → CacheFirst 30d; `/api/*` → NetworkFirst 5s; images (incl. R2 CDN) → StaleWhileRevalidate 7d; navigations → NetworkFirst → cached `/offline` fallback. `skipWaiting()` + `clients.claim()` for instant SW takeover. (3) `SwRegister` registers `/sw.js` **PRODUCTION-ONLY** (dev HMR untouched). (4) `/offline` page renders purely from cache (zero DB/auth/data-fetch). (5) `InstallBanner` — Android `beforeinstallprompt` capture + iOS share-menu tip + standalone/dismiss(localStorage) guards. (6) `scripts/generate-icons.ts` (sharp, already installed) builds icons + branded 390×844 screenshots from `getx-mark.webp`. (7) layout PWA metadata (manifest/appleWebApp/apple-touch-icon/msapplication) + `next.config.ts` serves `/sw.js` with `Service-Worker-Allowed: /` + `no-cache`. **CSP note:** the current CSP has no `script-src` directive yet (deferred to Step 32), so the Workbox CDN `importScripts` is allowed; when Step 32 adds `script-src`, it must allow `storage.googleapis.com` OR Workbox must be bundled locally to `public/`. **Deferred:** Web Push (VAPID keys + PushSubscription model + dispatch worker) → Phase 3. qa-step24.ts **36/36** + typecheck/lint/build green. Lighthouse PWA ≥90 = manual acceptance | Mobile-first is a core principle; an installable, offline-resilient app raises retention + re-engagement on the phone-first audience. Hand-rolling the SW avoids the next-pwa/Turbopack breakage and keeps full control of caching |

| 2026-06-10 | **Step 26 — AI demand forecast + pricing (built)**: a daily market pulse for sellers + the ops team. (1) **Schema** migration `20260610990000_step26_market_signals` — **`MarketSignal`** (daily per game+category-kind: orderCount/avgPriceMinor/searchCount, `@@unique([gameId,categoryKind,date])`) + `SearchLog` (append-only, no PII). **NAMING RECONCILE:** the name `DemandSignal` was ALREADY taken by the Prompt-12 demand-capture *email* model (a totally different feature) → used `MarketSignal` to avoid a clash. (2) **`/api/cron/demand-signals`** (fail-closed constant-time Bearer `CRON_SECRET`, idempotent upsert via raw `GROUP BY l."gameId", l."type"` over yesterday's COMPLETED orders, `vercel.json` `30 20 * * *`). (3) **`demand-forecast.ts`** — `aggregateMarketSignals` (callable directly so QA drives it w/o HTTP), `getDemandForecast` (Haiku → HIGH/MED/LOW + reasoning + 7-day outlook) and `getPricingRecommendation` (Haiku → RAISE/LOWER/KEEP + suggested price, `Math.round`+clamp ≤0→KEEP), both with a 1-hour in-memory TTL cache and **graceful degradation** (<3 signals → deterministic low-data fallback, no AI key/error → null; never crashes). (4) **Search logging** = fire-and-forget `logSearch(f.q, f.game)` in `searchListings` (`.catch(()=>{})`, blank skipped, ≤500 chars, never awaited). (5) **UI** — seller **Market Pulse** demand pills on `/seller/listings` (HIGH green / MED amber / LOW red / unavailable grey); **Trending searches** tag-cloud added to the EXISTING `/admin/analytics` (its GMV trend / top games / top categories were already built via audit Prompt-19 — not duplicated). **Note:** this pricing assistant (signal-driven) coexists with the Step-20 AI price button (peer-benchmark-driven) — different inputs, both advisory, neither auto-applies. qa-step26.ts **11/11** (aggregation correctness + idempotency + search-log skip + fallbacks) + typecheck/lint/build green. **To go live:** owner sets `ANTHROPIC_API_KEY` (forecasts) + `CRON_SECRET` (cron) | A live demand read turns sellers into operators (price into demand, list where buyers search) and tells the ops team which game/category to recruit supply for. Reconciling the model name + reusing the existing analytics page avoids schema clashes + duplicate dashboards |

| 2026-06-10 | **Step 27 — Community (built)**: the stickiness layer — guides, leaderboards, creator badges. (1) **Schema** migration `20260611000000_step27_community` — `Guide` (Markdown raw), `GuideView`/`GuideLike` (`@@unique([userId,guideId])`), `Badge` (code PK) + `UserBadge` (`@@unique([userId,badgeCode])` = idempotent) + `BadgeAwardedBy` enum; 5 badges seeded (idempotent upsert on code). (2) **`badges.ts`** — `awardBadge` (idempotent, never throws on re-award), `checkAndAwardMilestoneBadges` (TRUSTED_VETERAN ≥500 sales + EARLY_SELLER first-50, wired INTO `escrow.releaseOrder`'s tx right after the totalSales bump — cheap + idempotent, safe in the money tx), `checkAndAwardGuideAuthorBadge`, `awardTopSellerBadges` (monthly cron, `ROW_NUMBER() OVER (PARTITION BY game)` top-10). (3) **`guides.ts`** — CRUD + `incrementViewCount` (**`createMany skipDuplicates`** to detect a new view without a P2002 that would abort the tx; author's own views excluded), `toggleLike` (serializable check-then-act, likeCount floored at 0), `createGuide` (auto-publishes for TRUSTED_VETERAN ≥500 sales else draft-for-review; slug = kebab + random suffix; P2002→friendly error), publish/unpublish, `deleteGuide` (DRAFT only — published guides are unpublish-only for SEO), `getGameLeaderboard` (30-day completed-order raw rank). (4) **XSS:** guide Markdown rendered via react-markdown + remark-gfm + rehype-highlight ONLY — **no rehype-raw, no dangerouslySetInnerHTML** (raw HTML escaped). (5) **Reconciliations:** skipped **`@uiw/react-md-editor`** (heavy + React-19 peer risk) → plain textarea + live react-markdown preview (Write/Preview tabs, same UX, lighter); `/sellers/[id]` already existed (Step 13) → only ADDED a badge row; param `id` is the seller's userId. (6) **Pages:** `/guides`(+filter/pagination) + `/guides/[slug]` (view tracker + optimistic like), `/leaderboards`(+`[gameSlug]` medals + badge strip, ISR 3600), `/seller/guides`(+`/new` editor), `/admin/guides` (publish/unpublish, audit-logged), nav links (admin + seller sidebar, footer Community column). (7) **Cron** `/api/cron/award-top-sellers` (fail-closed Bearer, 503 if no `CRON_SECRET`, `vercel.json` `0 0 1 * *`). qa-step27.ts **18/18** (badge idempotency/milestones, guide draft-vs-autopublish, view idempotency + author-exclusion, like toggle, published-delete block, leaderboard ordering) + typecheck/lint/build green | Community is the "I live here" moat (vs "I shop here") — guides pull SEO + buyers, leaderboards + badges turn reputation into status, and milestone badges reward the behaviours (sales, guides) that grow the marketplace. The reputation graph compounds; it's the hardest thing for a competitor to copy |

| 2026-06-11 | **Step 28 — Algolia search (built, env-safe dormant)**: typo-tolerant faceted search, with **Postgres as the zero-config fallback**. (1) **`algolia.ts`** — `getAlgoliaAdminClient()` (null without `ALGOLIA_APP_ID`+`ALGOLIA_ADMIN_KEY`), `isAlgoliaConfigured()` (app id + `NEXT_PUBLIC_ALGOLIA_SEARCH_KEY`), `toAlgoliaRecord` (description truncated to 200, createdAt→unix seconds). **`algoliasearch` v5** — flat API (`searchSingleIndex`/`saveObject`/`deleteObject`), NOT v4 `initIndex`. (2) **`search-sync.ts`** — `syncListingToAlgolia` (ACTIVE→saveObject, else delete; null listing→defensive delete; wrapped try/catch, **never throws**) wired fire-and-forget (`void sync(id)`) into `createListing`/`updateListing`/`setListingStatus`/`removeListing`; `bulkSyncAllListings` (1000-record batches, per-batch try/catch, returns {synced,deleted,errors}). (3) **`/api/cron/algolia-sync`** (fail-closed Bearer, 200 "skipped" when unconfigured, `vercel.json` `0 3 * * *`) + one-off `src/scripts/algolia-setup-index.ts` (searchable attrs + facets + customRanking trust/rating + typo + price/newest replicas). (4) **`searchListings` Algolia path** — when configured, Algolia does query/typo/ranking + returns objectIDs → **Postgres hydrates the exact card shape in Algolia's order** (UI is backend-agnostic; same `MarketplaceResultPage`); on ANY error or no-keys → existing Postgres path UNCHANGED. URL param contract (`q/game/type/min/max/sort/page`) identical. (5) **`InstantSearchBar`** (client, `algoliasearch/lite` + public key, 350ms debounce, ≤5 suggestions) — renders **null without keys** so the existing server-side search remains. **Admin-key safety RECONCILE:** the spec wanted `import "server-only"` but that breaks `tsx` scripts + the QA harness (the package isn't resolvable / throws under tsx) → removed it; the admin key is protected anyway because Next inlines ONLY `NEXT_PUBLIC_*` into client bundles (so `ALGOLIA_ADMIN_KEY` is `undefined` in the browser), plus the import-discipline comment. qa-step28.ts **15/15** (config/null, mapper, sync no-op, Postgres fallback intact, cron fail-closed) + typecheck/lint/build green. **To go live:** owner provisions an Algolia app, sets the 4 keys, runs `npx tsx src/scripts/algolia-setup-index.ts`, then hits `/api/cron/algolia-sync` once to backfill | At launch the Postgres ILIKE search is fine; Algolia matters once listings grow (typo tolerance + sub-100ms + facets convert better). Building it dormant-but-ready means flipping it on is a keys-only change with zero risk to the current search |

| 2026-06-11 | **Step 29 — Sumsub automated KYC (built, env-safe dormant)**: replaces manual KYC with Sumsub's liveness + document verification; **manual upload stays the fallback** when keys are absent. (1) **Schema** migration `20260611100000_step29_sumsub` — `User.sumsubApplicantId` + `sumsubReviewedAt` (+ index). (2) **`sumsub-config.ts`** — `SUMSUB_ENABLED` snapshot + `isSumsubEnabled()` runtime-fresh check (webhook/service read `process.env` directly so QA + env changes are respected). (3) **`kyc-sumsub.ts`** — plain `fetch`, **HMAC-SHA256 signed** (`ts+METHOD+path+body`, same no-SDK pattern as CoinGate/Razorpay): `createApplicant` (idempotent — returns stored id without a 2nd API call; on first create flips kycStatus PENDING + audits in a tx), `generateSDKToken` (ownership-checked), `getApplicantStatus` (tolerant Zod, null on incomplete/error), and the shared **`applyKycReview`** (GREEN→APPROVED/RED→REJECTED). (4) **Webhook** `/api/webhooks/sumsub` — **FAIL-CLOSED**: no `SUMSUB_SECRET_KEY` or bad/missing `x-payload-digest` → 401 (raw-body HMAC, timing-safe); Zod 400; only `applicantReviewed` acted on; 500 on error so Sumsub retries; unknown types 200 no-op. (5) **IDEMPOTENCY RECONCILE:** the spec said use `ProcessedWebhook`, but its `provider` is the `PaymentProvider` enum (COINGATE/RAZORPAY) — adding SUMSUB would pollute payment semantics. Instead idempotency comes from the **KYC state-transition CAS** (`updateMany WHERE kycStatus != target`): a duplicate event finds the status already there → no flip, no 2nd AuditLog. Cleaner + race-safe (webhook vs poll). (6) **Actions** `getOrCreateApplicantAction`/`pollKycStatusAction` (SELLER-gated, graceful errors). (7) **UI** — `SumsubKycWidget` (client, lazy `@sumsub/websdk-react` via `dynamic ssr:false`; boots applicant+token, polls every 10s ≤5min after SDK onComplete; renders **null** when disabled → parent shows the manual R2-upload form + yellow banner); `/seller/verify` branches on `SUMSUB_ENABLED`; admin Users page gains a "View in Sumsub Cockpit ↗" deep-link + reviewedAt when an applicant id is present. **Admin manual `reviewKyc` override always works regardless of Sumsub state.** qa-step29.ts **19/19** (GREEN/RED/dup/bad-sig/missing-sig/unknown-type/bad-body/idempotent-create/no-keys-fallback — webhook fully tested with an in-process test secret + real HMAC) + typecheck/lint/build green. **To go live:** owner sets `SUMSUB_APP_TOKEN` + `SUMSUB_SECRET_KEY`, configures a `basic-kyc-level` + the webhook URL in the Sumsub dashboard | Manual KYC doesn't scale + has fraud/latency gaps; Sumsub automates liveness + doc + AML screening with an instant verdict. Building it env-safe means it flips on with keys only, and the state-machine idempotency avoids polluting the payment-provider enum |

| 2026-06-11 | **Step 31 — Observability (Sentry + PostHog; built, env-safe)**: production visibility, privacy-first. **Sentry** was already solidly wired at Step 09 (modern `instrumentation-client.ts` + `sentry.server/edge.config.ts`, `withSentryConfig` in next.config, `tracesSampleRate` 1.0 dev / 0.1 prod, empty-DSN = silent) — so this step only ADDED: `SentryUserSync` (client `setUser({id,email})` on mount + `setUser(null)` on unmount, in the dashboard layout), `SentryErrorBoundary` + branded `DefaultFallback` (wraps the dashboard; `src/app/error.tsx` route boundary covers listing/checkout + all routes; `global-error.tsx` already existed), and an ADMIN-only `/api/debug/sentry-test`. **PostHog** (new, fully env-safe — no `NEXT_PUBLIC_POSTHOG_KEY` ⇒ no provider, no init, all events no-op): client `PostHogProvider` (renders children directly when no key) + manual SPA `PostHogPageview` (Suspense-wrapped for `useSearchParams`) in the root layout; server singleton `getPostHogServer()` (`flushAt:1`/`flushInterval:0` for serverless) + `captureServerEvent()` helper (null-safe, never throws into a money/render path). **6 events** at real call sites: `listing_viewed` (listing page), `checkout_started` (createOrder — NEW orders only, not idempotent reuse), `payment_initiated` (startPaymentAction), **`order_completed` (escrow.releaseOrder — server-truth, never client-fired)**, `seller_onboarded` (becomeSeller — new profile only), `search_performed` (marketplace.searchListings wrapper — query truncated to 100, distinctId always `"anonymous"`). **PII POLICY (enforced):** event properties carry **ONLY IDs + integer amounts** — never name/email/phone/username; `$ip:null` forced on every server event; comment block in `src/lib/posthog.ts`. qa-step31.ts **12/12** (env-safe null singleton, no-op capture, query truncation, zero-PII on all 6 shapes, Sentry no-throw, debug admin-gate) + regressions 08/21/28 green + typecheck/lint/build clean. **To go live:** owner sets `NEXT_PUBLIC_POSTHOG_KEY` (+ host) and the Sentry vars (`SENTRY_AUTH_TOKEN`/`ORG`/`PROJECT`/`NEXT_PUBLIC_SENTRY_DSN`), then configures two Sentry alert rules (new-issue → email; error-rate > 1%/10min → email/Slack) | You can't fix what you can't see; Sentry catches the errors, PostHog measures the funnel (view→checkout→pay→complete). Building it env-safe + PII-free means it switches on with keys only, stays GDPR-friendly, and analytics can never break a money path |

| 2026-06-11 | **Step 32 — Security hardening (built, env-safe)**: brute-force, XSS, session-revocation + webhook IP. (1) **Rate limiting** — kept the SYNC in-memory `rateLimit` (25+ call sites unchanged) and ADDED async **`rateLimitDistributed`** (`@upstash/ratelimit` sliding-window, one `Ratelimit` cached per `limit:windowMs` combo, prefix `getx:rl`). **ENV-SAFE:** no `UPSTASH_REDIS_REST_URL`/`_TOKEN` ⇒ delegates to the in-memory limiter; on Upstash error ⇒ **fail-OPEN** to in-memory (a Redis blip must never lock everyone out). Migrated only the brute-force surface to `await` it: register/login(perIp+perAccount)/forgot/reset (`actions/auth.ts`), credentials `authorize()` (`lib/auth.ts`), `pay-start` (`actions/payments.ts`). (2) **CSP** — a strong **STATIC** policy now ships on EVERY route via the Edge proxy (`lib/csp.ts` builder: default-src 'self', script 'self'+'unsafe-inline'(+dev 'unsafe-eval')+Turnstile+Razorpay, connect allowlist for PostHog/Sentry/socket/Razorpay from env, img 'self' data: blob: https:, object-src 'none', frame-ancestors 'none', base-uri 'self', form-action 'self', prod upgrade-insecure-requests). **Removed CSP from next.config** so each route emits exactly ONE header (no intersection). **Nonce mode** (per-request `btoa(crypto.randomUUID())` + 'strict-dynamic' on sensitive routes, Next picks up `x-nonce`) is fully built but **DORMANT behind `CSP_NONCE_ENABLED`** — OFF by default because (a) it forces dynamic rendering, breaking the ISR/SEO strategy on public pages, and (b) it would block the root-layout JSON-LD + needs checkout/Razorpay/OAuth runtime-verification first. Flip on at launch after verification (same dormant-until-launch pattern as Upstash/Algolia/Sumsub). (3) **Session revocation** — `User.sessionVersion` (migration `20260611200000_step32_session_version`); the jwt callback embeds it at sign-in (credentials AND OAuth via the `user?.id` branch) and **bounded-re-checks** it (≤`SESSION_REVOCATION_CHECK_MS`=60s per active session, not every request — Neon cost) → returns **null** (signs out) on mismatch/user-gone, **fail-OPEN** on a transient DB error (no mass-logout on a blip). `sessions.ts#invalidateUserSessions` returns a PrismaPromise so it drops ATOMICALLY into the existing array `$transaction` of `setUserBanned`(ban only)/`setUserRole`; `resetPassword` folds the increment in. (4) **Webhook IP allowlist** (`config/webhooks.ts`, env lists parsed at module load, **OPEN by default** + boot warn until set) checked at the top of the Razorpay + CoinGate POST handlers (403 before sig/token). Caveat documented: behind Cloudflare orange-cloud XFF is the proxy IP → DNS-only or allowlist egress. (5) **`npm audit`** = 0 high/critical; 4 moderate are postcss-bundled-inside-next (build-time CSS stringify, not a runtime XSS vector; "fix" = downgrade next to 9, refused) — accepted. **19-agent adversarial review** (find→verify): 12 raw findings → 6 real fixes applied (clientIpFromHeaders prefers Cloudflare `CF-Connecting-IP`; webhook allowlist reads env at CALL time for testability; `vitest.setup` refuses a non-disposable `TEST_DATABASE_URL`; middleware matcher skips static/dotted assets so getToken doesn't run per-image; `limiters` Map cap; `csp.ts` warns on unparseable origin) and 5 rejected as false-positives WITH library-source proof — notably "proxy.ts isn't wired" (FALSE: Next 16 compiles `src/proxy.ts`→`.next/server/middleware.js`, build prints "ƒ Proxy (Middleware)") and "returning null from the jwt callback crashes the session callback" (FALSE: `@auth/core` guards `if (token !== null)` before `callbacks.session` and `else` clears the cookie → clean sign-out); the 60s revocation latency is the documented bounded-check tradeoff. qa-step32.ts **32/32** + typecheck/lint/build green. **To go live:** owner sets `UPSTASH_REDIS_REST_URL`+`_TOKEN`, optionally the `*_WEBHOOK_IPS`, and — after verifying checkout/OAuth — `CSP_NONCE_ENABLED=true`. Cloudflare WAF (managed rules + rate rules on /api/auth + /api/webhooks) is a dashboard step at deploy (Step 35) | Real money + accounts demand defense-in-depth, but a junior solo dev can't risk a CSP that silently breaks checkout. Shipping a strong static CSP NOW + a dormant nonce upgrade, a distributed limiter that degrades to in-memory, and a fail-open revocation check is the balance: stronger today, strictly stronger at launch, never a broken page or a mass-logout |

| 2026-06-11 | **Step 33 — Performance (mostly reconciliation)**: a 4-agent codebase map found the app already ~90% optimized (composite indexes exceed the spec, hot `include`s have no N+1, ISR timings tuned, Poppins weights minimal, JetBrains-Mono `preload:false`, bundle-analyzer wired at Step 24, recharts already isolated in `"use client"` chart files = route-split). So this step closed only the real gaps: `images.formats: ["image/avif","image/webp"]`; `/about` + `/how-it-works` → `revalidate=false` (fully static); promoted **sharp** to a direct dependency (Vercel `next/image` runtime); removed unused **framer-motion**. **Reconciliations logged, NOT "fixed":** `/listing/[slug]` stays `revalidate=60` (freshness > spec's 300); the spec's `Order.completedAt` / `LedgerEntry[userId,type]` indexes don't apply (our model is `status=COMPLETED`+`updatedAt` and wallet-based `[walletId,reason,type,createdAt]`); recharts `dynamic()` skipped (analytics page is `force-dynamic` + charts already client-split → no win). Lighthouse/CWV is a CI/manual step. qa-step33.ts **19/19** asserts the reconciled state as executable regression checks | Chasing a perf checklist that's already done wastes runway and risks breaking working code. Map first, close the few real gaps, encode the "already-optimized" facts as regression assertions |

| 2026-06-11 | **Step 34 — Testing (built; integration/E2E dormant)**: first real test harness beyond `qa-step*.ts`. (1) **Vitest** (node default + per-file `jsdom` opt-in, `@`→`src` alias; `vitest.setup.ts` loads jest-dom + aliases `DATABASE_URL`←`TEST_DATABASE_URL` before any db import). (2) **39 unit tests** over the PURE money/security math against REAL signatures (reading the source caught the map being wrong — trust-score uses `Math.round` and scores a null reply-time as the neutral **10**, not 0): `fees` (round-half-up, qty clamp, shield/level/PRO floors), `encryption` (AES-GCM round-trip, tamper reject, fail-closed), `trust-score` (hand-computed breakdowns + KYC-gated levels), `orders` (state-machine edges + terminals), `loyalty` (floored points), + a jsdom toolchain smoke test. (3) **Coverage gate** scoped to the 3 fully-pure modules at **80%** (achieves 100%) — honest, not a blunt global number the RSC/UI layer would fail. (4) **Money-path integration suite** (createOrder→pay→deliver→confirm + refund/restock/idempotent-noop, asserting the escrow ledger via `getWalletBalances`) — `describe.skipIf(!TEST_DATABASE_URL)`, **dormant**, never touches dev/prod data; authored against verified qa-step10 contracts, typecheck-validated, runs in CI against ephemeral Postgres. (5) **Playwright** (chromium + Pixel-7) + **E2E specs** (marketplace, auth route-protection exercising the REAL proxy redirect, mobile drawer) — dormant until `playwright install` in CI. (6) **`.github/workflows/ci.yml`** — quality (typecheck+lint+coverage, no DB) → build-integration (postgres:16 service + migrate+build+integration) → e2e (postgres+seed+playwright). **No external accounts** — a throwaway Postgres service container is the test DB. `npm run test` = **39/39**, coverage 100% on gated modules. **Deferred per the standing constraint:** local integration needs a `TEST_DATABASE_URL` (Neon branch) at launch | A money marketplace needs a real regression net, but the unit gate must be honest (pure logic at 100%, not a vanity global %), and integration/E2E must stay dormant until a disposable test DB exists — never against live data. Reading real signatures first caught a map error (null reply-time = 10) a guessed test would have baked in wrong |

| 2026-06-11 | **Step 25 — AI Dispute Judge (built, env-safe)**: `claude-opus-4-8` arbitrates escrow disputes; high-confidence verdicts auto-resolve, the rest go to an admin with a 1-click accept/override. (1) **Model call** reuses `ai.ts#generateJSON` (Zod-validated, retry-once) at `AI_MODELS.reasoning = claude-opus-4-8` — **NOT the spec's `messages.create` with `temperature: 0`**: Opus 4.8 REMOVED `temperature`/`top_p` (they 400), so passing temperature would break every call. The judge reads order + listing + delivery proof + last-30 chat + buyer↔seller reviews → `{verdict: BUYER|SELLER, confidence 0-100, reasoning, keyFacts}`; a parse failure → SAFE_DEFAULT (BUYER/refund, confidence 0 → human review — never auto-pays a seller on a glitch). (2) **Confidence gate** (≥70 → auto-resolve) calls the EXISTING `escrow.resolveDispute` — the single source of money truth; the judge never writes a ledger row. resolveDispute's `adminUserId` was widened `string → string | null` (AuditLog.actorId is already nullable) so an AI resolution records a null/system actor cleanly. Persist-AI-fields-then-resolve is two writes (not one tx) ON PURPOSE: if resolve fails the AI suggestion is still saved on an un-resolved dispute for the admin (matches the spec's failure path); resolveDispute's OPEN-status CAS makes a concurrent admin/AI double-resolution impossible. (3) **pgvector case memory** — `previewFeatures=["postgresqlExtensions"]` + `extensions=[vector]`, `DisputeEmbedding(embedding Unsupported("vector(1536)"))` + ivfflat(lists=50) cosine index; migration `20260611210000_step25_dispute_judge` runs `CREATE EXTENSION vector` on the DIRECT_URL (idempotent, Neon supports it). Written/read via `$executeRaw`/`$queryRaw` with the `<=>` operator (Prisma can't map the vector column); the vector literal is built from JS NUMBERS only (`[${vec.join(",")}]`), so no injection. (4) **`embeddings.ts`** — keyword-hash vector (djb2 → 1536 buckets, L2-normalised) by DEFAULT so retrieval works offline with no dependency; OpenAI `text-embedding-3-small` via raw `fetch` (no npm package added) only when `OPENAI_API_KEY` is set; `embedText` NEVER throws (degrades to keyword on error). (5) **Background enqueue** = `setTimeout(0)` (documented TODO: replace with a durable queue — serverless can drop it), **no-op without `ANTHROPIC_API_KEY`** so the whole judge is dormant and admins resolve manually exactly as before. (6) **Admin UI** — AI verdict badge + "Needs Review" pill on the queue, an "AI Analysis" panel (verdict, confidence bar, reasoning, key facts, "Auto-resolved by AI" banner) with Accept / Override (reason) on the detail page; both ADMIN-gated + rate-limited server actions, audit-logged (`ACCEPT_AI_VERDICT` / `OVERRIDE_AI_VERDICT`), and human overrides store the corrected case as a high-quality embedding for future few-shot retrieval. **Reconciliations:** `resolveDispute` takes orderId (not disputeId), outcome `REFUND_BUYER`/`RELEASE_SELLER`, OrderDelivery.`content` (not deliveryNote/proofUrls), Review.`rating` (not ratingScore), no `Order.completedAt`; override of an ALREADY-SETTLED dispute records the correction but does NOT auto-reverse the ledger (a settled escrow move needs a manual compensating entry — out of MVP scope). A `setJudgeModelOverride` TEST SEAM lets QA inject deterministic verdicts without a live key (null in prod). qa-step25.ts **29/29** (pgvector ext, 1536-dim embeddings, low→human / high→auto-resolve with correct ledger, embedding stored, similar-case retrieval, idempotency, accept, override+corrected-embedding, no-key throw) + typecheck/lint/build green. **21-agent adversarial review (find→verify) → 8 fixes:** (1) a durable **`/api/cron/judge-disputes`** (*/10) backstop — `setTimeout(0)` can be dropped by a frozen serverless lambda, so the cron re-judges any OPEN dispute with `judgedAt IS NULL` (dropped fast-path job = delayed ≤10min, never lost); (2) `judgeActorType="AI"` is now folded into `resolveDispute`'s transaction (5th optional param) so status + actor + money move ATOMICALLY (no crash window); (3) AI fields persisted via a `WHERE status=OPEN` CAS so a concurrent admin resolution can't be clobbered; (4) the idempotency early-return now reports `autoResolved` truthfully from `judgeActorType` instead of always false; (5) accept/override actions revalidate `/admin/disputes/[orderId]` + `/orders/[orderId]` (not just the list); (6) cosine similarity clamped to [0,1] via `GREATEST` (OpenAI vectors can be negative); (7) OpenAI embedding response validated element-wise (finite numbers) before building the `::vector` literal; (8) the test seam is hard-guarded to no-op in production. Rejected as by-design/cosmetic: SAFE_DEFAULT→human-review (dispute stays OPEN, never auto-pays), ivfflat lists=50 (planner seq-scans tiny tables), settled-override-no-reversal (documented scope, audit-flagged). **To go live:** owner sets `ANTHROPIC_API_KEY` (judge activates); optional `OPENAI_API_KEY` upgrades retrieval to semantic embeddings | A 24/7 impartial first-pass on disputes cuts resolution latency (the #1 trust killer in a marketplace) while the confidence gate + admin accept/override keeps a human on every money decision. Routing money ONLY through the audited escrow service (never a duplicate ledger path) and defaulting to refund-on-uncertainty means the AI can speed things up but can never silently mispay |

| 2026-06-11 | **Step 23 — i18n (next-intl: English + Hindi) — FOUNDATION built, URL-routing activation DEFERRED to post-deploy**: a deliberate CTO scope call. The spec's design needs an app-wide structural refactor — move every page route under `src/app/[locale]/` for `/hi/` URL routing + hreflang — AND re-chain next-intl's middleware with the just-hardened `proxy.ts` (CSP + auth, Step 32). Three things converged against doing that surgery right now: (a) **a Windows file-lock** blocked the `git mv` of the `(auth)` route group mid-restructure (the move is not reliably completable in this environment → risk of a half-moved, broken app); (b) the no-move alternative (next-intl **without** i18n routing, locale from a cookie) reads `cookies()` in `getRequestConfig` which **opts every page into dynamic rendering** — globally undoing the Step 33 ISR/static-SEO optimization, right before deploy; (c) **CLAUDE.md §3 lists i18n as "Add later, phase-wise (NOT at MVP)"** and the next milestone is Step 35 (deploy). So I shipped the **durable ~80%** that is safe + reusable and DEFERRED the routing wiring + string conversion. **Built now (all inert/safe — does not touch routing, the proxy, or the build):** `next-intl` installed; `src/i18n/{routing,request,navigation}.ts` (locales `en`+`hi`, `localePrefix:'as-needed'`, missing-key fallback → key path + Sentry/console warn so a translation gap never 500s a page); **complete `messages/en.json` + `messages/hi.json`** (12 namespaces, ~180 keys each, identical key sets, accurate natural Devanagari — the real translation work); `src/lib/format.ts` (`formatCurrency`/`formatDate`, `hi-IN`/`en-IN` aware — usable immediately); a ready `LanguageSwitcher` component (built, NOT yet mounted). qa-step23.ts **13/13** (en/hi key parity, no-empty-values, Devanagari-presence, ICU-placeholder parity, locale formatting) + typecheck/lint/build green (foundation is inert → deploy-ready build unchanged). **POST-DEPLOY ACTIVATION CHECKLIST** (do with the dev server + editors closed so the file-lock clears, ideally on a clean checkout, and runtime-verify each step): (1) `git mv` all page routes (route groups + admin + offline + error.tsx + not-found.tsx) under `src/app/[locale]/`; keep `api/og/robots.ts/sitemap.ts/global-error.tsx` at root. (2) Re-add `withNextIntl = createNextIntlPlugin('./src/i18n/request.ts')` to next.config + wrap the export. (3) Build `src/app/[locale]/layout.tsx` from the old root layout: `generateStaticParams` (both locales → keeps pages static per-locale, no ISR loss), `setRequestLocale(locale)`, `<html lang={locale}>`, wrap children in `<NextIntlClientProvider messages={await getMessages()}>`, and `metadata.alternates.languages` for hreflang; delete `src/app/layout.tsx`. (4) Chain `createMiddleware(routing)` into `proxy.ts` FIRST (locale detect + `/en`→`/` redirect + `/hi` rewrite), then layer the existing auth checks (strip the `/hi` prefix before the protected-path matching; make redirect targets locale-aware) + the CSP headers onto next-intl's response; widen the matcher. (5) Mount `<LanguageSwitcher>` in the header + mobile nav. (6) Convert hardcoded JSX text → `t()` (`useTranslations`/`getTranslations`) and `next/link` → `@/i18n/navigation` Link, priority chrome first (header/footer/nav/home/auth) then deeper pages incrementally; seller-entered content (titles, descriptions, reviews, usernames) is NEVER passed through `t()`. (7) `sitemap.ts` emits both locale variants. (8) Extend `scripts/qa-step23.ts` with the runtime checks (`/hi` renders 200 no MISSING_MESSAGE, hreflang in head, `/en`→`/` redirect, `NEXT_LOCALE` cookie persistence) against `npm run dev` | Doing an app-wide route restructure + middleware re-chaining that I can't runtime-verify, immediately before deploy, on a filesystem actively locking the move, is the single riskiest change I could make to a deploy-ready codebase. The durable value of i18n is the translations + config + formatting (done); the wiring is mechanical and belongs in a focused, runtime-verified pass after launch — exactly where CLAUDE.md already files it |

## What we are NOT using (and why)
- **Stripe** — game-account sellers ko ban/hold karta hai (biggest payment risk)
- **Medusa / Mercur** — solo junior MVP ke liye bahut complex; e-commerce baggage gaming me kaam ka nahi
- **Supabase** — abhi zaroorat nahi; Neon+Prisma kaafi
- **Render** — cold starts = bad buyer experience
- **Redis (at MVP)** — Vercel Cron se kaam chal jayega; baad me agar BullMQ chahiye to add karenge

## Add later (phase-wise, NOT at MVP)
Algolia (search) · Sumsub (KYC/AML) · TalkJS (agar Socket.io kam pade) · PostHog + Sentry ·
Resend (email) · Tremor + Recharts (seller dashboard) · next-intl (i18n) · loyalty/referral · PWA.
